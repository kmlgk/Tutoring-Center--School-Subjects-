# Template Design Registry

Master log of every unique template generated with the Premium HTML Template Generator prompt. Consult this file before starting a new template — no layout, palette, hero composition, header, navigation, card structure, section order, footer, animation concept, or typography pairing may repeat.

---

## Template 01 — Bakery & Cake Shop

- **Brand name:** Bloom & Batter Cake Studio
- **Tagline:** "Handcrafted Cakes for Life's Sweetest Moments"
- **Business category:** Bakery / custom cake shop
- **Primary color:** Raspberry #C9184A (600)
- **Secondary color:** Caramel #F4A261 (500)
- **Neutral / background:** Cream #FFF8F0, Chocolate-brown dark palette #1F150E–#5A3E2C
- **Font combination:** Fraunces (display serif, headings) + Caveat (script accents/taglines) + Poppins (body)
- **Header type:** *(revised post-launch — see changelog below)* Logo-left, single unified nav row, controls-right — permanent glass background (light/dark) on every page, fixed position, no hide-on-scroll. Desktop nav + CTA appear at `xl` (1280px); hamburger drawer covers everything below that, including tablet landscape (1024–1279px).
- **Navigation style:** One flex row: logo (shrink-0) → all nav links in a single centered group (`flex-1`) → theme/RTL toggles + CTA + hamburger (shrink-0). 7 links: Home, Home 2, About, Menu, Gallery, FAQ, Contact.
- **Hero layout (Home):** Full-bleed photo hero with dual blurred "frosting blob" floating shapes, badge pill, large serif headline with script word accent, dual CTA buttons, animated scroll-down chevron, floating stats card overlapping hero bottom edge (CountUp numbers).
- **Hero layout (inner pages):** Compact (not full-height) banner with breadcrumb, varies per page: photo+gradient overlay (About, Contact, Gallery), solid raspberry with sprinkle pattern (Menu), gradient raspberry→choco with sprinkle pattern (Order), solid caramel with sprinkle + blob (FAQ). No two inner heroes are visually identical.
- **Section order (Home):** Header → Hero → Floating stats card → Why Choose Us (icon grid) → Cakes for Every Occasion (image category grid) → Today's Menu Highlights (product cards) → Gallery preview (asymmetric grid + lightbox) → Testimonials (Swiper carousel) → Process timeline (4-step, dark section) → CTA banner (gradient) → Footer.
- **Card style:** `cake-card` — rounded-3xl white/choco-800 cards, soft raspberry-tinted shadow on hover (`shadow-soft`), signature "scalloped fondant-edge" CSS motif (radial-gradient repeating bumps) used on section dividers and footer top edge.
- **Button style:** Pill / fully rounded. Primary = raspberry→pink gradient fill with lift + glow shadow on hover. Outline = 2px border, fills solid on hover.
- **Image style:** Full-bleed hero photography, image category cards with dark gradient overlay + hover zoom, masonry/columns gallery, flip-card (3D rotateY) team portraits on About.
- **Background style:** Cream base with a subtle repeating sprinkle dot-pattern texture (SVG radial-gradient) alternating with solid raspberry/caramel/choco full-bleed sections.
- **Animation style:** AOS scroll-reveal (fade-up/zoom-in) site-wide; CSS keyframe floating blobs in hero + 404; CountUp.js animated stat counters; Swiper.js autoplay testimonial carousel; GLightbox photo lightbox; 3D CSS flip-cards for team bios; smart-scroll header.
- **Footer layout:** Dark chocolate-brown, scalloped top edge, 4-column grid (brand+social / quick links / occasions / newsletter form with inline validation), bottom bar with copyright + address.
- **Mobile menu pattern:** Right-side slide-in drawer (translateX, RTL-aware direction flip) with backdrop, stacked links, theme+direction toggle buttons, and a CTA button pinned above contact details.
- **Special design features:** Dark/light mode toggle (localStorage), RTL/LTR direction toggle (localStorage, flips drawer + icons via Tailwind `rtl:` variants), custom cake enquiry form with pickup-date picker (min=today, past-date validation) and live character counter, occasion-based filter tabs (menu + gallery, shared JS), anchor-linked occasion sections, vertical milestone timeline (About), flip-card team bios, breadcrumb nav on every inner page, bakery-themed 404.
- **Pages built:** index.html (Home), home-1.html (Home 2), about.html, menu.html, gallery.html, order.html (custom cake enquiry), faq.html, contact.html, 404.html.
- **Libraries used:** Tailwind CSS (CDN, class dark mode), Font Awesome 6 (icons), Google Fonts (Fraunces/Caveat/Poppins), AOS 2.3.4 (scroll reveal), Swiper 11 (testimonials + Home 2's bestseller carousel), GLightbox (gallery lightbox), CountUp.js 2.8 (animated stats, Home only). No build step; no backend.

**Do not reuse for future templates:** logo-left unified-nav header, raspberry/caramel/cream/choco palette, Fraunces+Caveat+Poppins pairing, scalloped fondant-edge divider motif, floating-blob hero with overlapping stats card, sprinkle dot-pattern background, right-side slide-in drawer with this exact control layout, flip-card (rotateY) team bios, split-screen hero with inline stat trio (Home 2), circular category avatars (Home 2), zig-zag alternating process steps (Home 2), horizontal scroll-snap galleries (Home 2), testimonial-spotlight-with-avatar-switcher (Home 2).

### Header component changelog (applies to all pages in this template)
The header went through two corrective revisions after initial launch — noted here so future templates don't repeat the same mistakes:
1. **Grid → Flex:** original header used `grid-cols-3` (three *equal* thirds: left-nav / centered-logo / right-nav+controls). Equal-thirds tracks don't grow to fit content — the right column (nav + icons + CTA) needed far more than 1/3 of the header width at realistic desktop sizes, overflowing and visually overlapping the centered logo. Replaced with a single flex row (logo `shrink-0` + nav `flex-1` + controls `shrink-0`) sized by actual content, not arbitrary fractions.
2. **Breakpoint lg → xl:** even the corrected flex layout was razor-thin at 1024px (`lg`) with 6 nav links; adding a 7th link (Home 2) would have overflowed again. Desktop nav/CTA now gate on `xl` (1280px) instead of `lg` (1024px) — tablet-landscape widths (1024–1279px) now correctly get the hamburger drawer instead of a cramped nav row. `DESKTOP_BREAKPOINT` in `main.js` was updated to match (1280).
Also fixed: the header's transparent-over-hero state (home page only) was replaced with a permanent semi-opaque glass background on every page, since dark nav text over a busy/dark hero photo was unreadable; and the mobile drawer's RTL "closed" position (`translateX(-100%)`) was removed entirely because it wasn't matched by an equal flip of the drawer's physical docking side (`right-0`), which pulled the panel partially into view the instant RTL was toggled — the drawer now always docks and slides from the right regardless of direction.

---

## Home Page 1 — `index.html` (existing entry, unchanged) vs. Home Page 2 — `home-1.html`

`home-1.html` ("Home 2" in nav) is an alternate homepage for the same brand — same tokens (color/type/header/footer/buttons/cards), deliberately different layout, hero composition, and section presentation from `index.html` per the master prompt's "Home 1 vs Home 2 must not share section layouts" rule.

- **Hero layout:** Split-screen (not full-bleed). Text column left (eyebrow badge, serif headline with one script word, paragraph, dual CTAs, inline trio of stat callouts separated by vertical dividers — no floating card). Image column right: single rounded-corner photo with a small overlapping "review quote" card anchored to its bottom-left corner (hidden below `sm` to avoid crowding).
- **Section order:** Header → Hero (split-screen) → Cake Categories (circular avatar row, horizontally scrollable on mobile) → Best-Selling Cakes (Swiper carousel, peek-next-slide on mobile) → This Week's Fresh Bakes (dark icon band, 7-day highlight strip) → Custom Ordering Process (4-step zig-zag: alternating image-left/text-right and text-left/image-right, not the horizontal icon row used on Home) → From Our Gallery (horizontal scroll-snap strip, no lightbox) → Testimonials (single large spotlight quote + clickable initials-avatar switcher, not a multi-up carousel) → Seasonal Offers (2 gradient/dark promo cards, icon watermark, no photos) → CTA (image-left / dark-panel-right split, not a centered gradient banner) → Contact/Store info (compact 3-column icon strip linking to the full Contact page) → Footer (identical, shared component).
- **Image treatment:** Circular "rounded image" category avatars (new pattern for this template); horizontal scroll-snap "collage" strips for both the product carousel and the gallery preview (new interaction pattern); zig-zag alternating photo/text rows for the ordering process (vs. Home's numbered-icon-only row); no lightbox on this page (kept lightweight — full lightbox experience lives on `gallery.html`).
- **Animation:** Same AOS vocabulary as the rest of the site (fade-up/fade-left/fade-right/zoom-in), a second independent Swiper instance (`.bestseller-swiper`, peek-slide autoplay) distinct from Home's `.testimonial-swiper`, and a new lightweight vanilla-JS testimonial-switcher module (`data-testimonial-widget` / `data-testimonial-trigger` / `data-testimonial-panel` in `main.js`) — no new external library added for it.
- **Special features:** All category/CTA links route into `menu.html`'s hash-driven filter system (`menu.html#anniversary`, `#corporate`, etc.), so "shop by category" from this page filters the Menu grid exactly like it does from Home and from Menu's own category cards. Best-seller and gallery-strip content intentionally reuses the exact same products/photos already defined on `menu.html`/`gallery.html` (same name, price, and image per item) to keep pricing and imagery consistent across pages rather than inventing parallel data.
- **New nav entry:** "Home 2" added to both desktop nav and the mobile drawer on all 9 pages, linking here.

---

## Template 02 — Daycare & Kids Preschool

- **Brand name:** Little Sprouts Early Learning
- **Tagline:** "Nurturing Little Hearts with Loving Care" (Typed.js cycles: Loving Care / Early Learning / Bright Futures / Joyful Days)
- **Business category:** Daycare / kids preschool (infant through Pre-K, ages 6 weeks–5 years)
- **Primary color:** Coral #FF6B6B (500) — CTAs, accents
- **Secondary color:** Sky Blue #3DB4F2 (500) + Sunshine Yellow #FFC93C (500) + Leaf Green #06D6A0 (500) — playful 4-hue accent system (no single secondary; hue rotates by section/card)
- **Neutral / background:** Cream #FFFDF6, dark-mode Indigo/Navy #0D1321–#16121F (deliberately not brown/choco like Template 01)
- **Font combination:** Baloo 2 (rounded display, headings) + Quicksand (rounded sans, body) — no script font, 2-font pairing (different structure from T1's serif+script+sans triple)
- **Header type:** *(revised post-launch — see changelog below)* Two-level header — thin dark utility bar (phone/hours/enrollment link/social, hides on scroll-down) above a **logo-left, centered-nav** main row: logo (shrink-0, always far left, same on mobile) → nav links (`flex-1`, `justify-center`) → theme/RTL toggle + CTA (shrink-0, right) → hamburger (mobile only, `ml-auto` so it pins right regardless of what's hidden). Desktop nav gates at `xl` (1280px) per the lesson in T1's changelog.
- **Navigation style:** Logo-left with a centered nav group (not split either side of the logo — see changelog). 7 links total: **Home** (dropdown: Home 1 / Home 2), About, Programs, Gallery, Teachers, **Pricing**, Contact — FAQ reachable via footer + mobile drawer only (not crammed into the main desktop nav). The Home dropdown opens on hover *or* click (click never toggles closed — only outside-click/Escape/focus-out/mouseleave close it, avoiding a hover-then-click race condition), and mirrors as an inline accordion (chevron-button + collapsible sub-list) inside the mobile drawer.
- **Hero layout (Home):** Layered image composition — large organic **blob-mask** photo (CSS `clip-path: url(#id)` referencing an inline SVG `<clipPath clipPathUnits="objectBoundingBox">`, not a fixed-pixel `path()`, so it scales correctly at any size) with a smaller second blob-mask photo overlapping its bottom-left corner; floating SVG cloud/star decorative shapes (gentle bob keyframe, not blurred blobs); badge pill; Typed.js word-cycle headline; dual CTA; floating "State Licensed" card pinned to hero image corner (no CountUp stats card — that pattern is reserved for T1).
- **Hero layout (inner pages):** Compact banner with breadcrumb, varies per page: photo+gradient overlay (About, Contact), solid/gradient sky-blue with confetti-dot pattern (Programs), gradient coral with confetti (Gallery), gradient leaf-green with confetti (Teachers), gradient sunshine with confetti (Admissions), solid dark navy with confetti (FAQ). Every banner ends in a signature **wavy SVG divider** (this template's motif, replacing T1's scalloped edge) whose curve direction/color varies per section transition.
- **Section order (Home 1 — `index.html`):** Header → Hero (layered blob images) → Trust badges strip (4 icons) → Welcome snapshot (split image+text, floating stat badge) → Age-Group Programs (4-tab switcher: Infant/Toddler/Preschool/Pre-K, image+text panel) → Daily Schedule (vertical alternating timeline with dashed rail + colored icon nodes) → Why Parents Choose Us (6 sticker-card icon grid) → **Safety & Hygiene** (split photo-left / numbered-protocol-list-right, new section) → Facility Gallery preview (horizontal-scroll polaroid filmstrip, rotated photos, no lightbox) → Meet Our Teachers preview (Swiper circular-avatar carousel, click-to-modal) → Parent Testimonials (single spotlight quote, Swiper fade effect, no multi-card carousel) → **Upcoming Events** (3-card grid, "ticket stub" motif — date block + dashed-line divider + details, new section) → **Enrolment Process** (horizontal 4-step band, white circles on a solid sky-blue section connected by a dashed line, new section) → **FAQ preview** (single-open accordion, top 4 questions + "View All FAQs" link, new section) → Enrollment CTA (image-left / coral-panel-right split) → Footer.
- **Card style:** `sticker-card` — rounded-[1.75rem] white/navy cards, **thick 3px colored border** (color varies per card via `--card-accent`), slight independent rotation per card (`--rot`, corrects to 0° + lifts on hover), small rotated "tape" rectangle pinned at top-center. Completely different silhouette from T1's scalloped-edge `cake-card`.
- **Button style:** `btn-chunky` — chunky rounded-xl (not pill), solid fill with a **thick flat bottom-offset shadow** (`box-shadow: 0 5px 0 <darker-shade>`) that compresses on `:active` (translateY + shadow shrink) for a tactile "press" feel — mechanically distinct from T1's pill gradient lift+glow.
- **Image style:** Organic blob-mask hero photography (objectBoundingBox SVG clip-path), rotated polaroid-frame photos (gallery filmstrip + preview), circular cropped teacher/testimonial portraits, editorial CSS-grid gallery (asymmetric spans, not masonry columns) with hover-zoom + caption overlay. No flip-cards anywhere (reserved for T1).
- **Background style:** Alternating pastel color-block sections (cream / sky-tint / leaf-tint / coral-tint / sunshine-tint) separated by wavy SVG dividers; sparse confetti-dot pattern reserved for inner hero banners only (not a site-wide texture like T1's sprinkle background).
- **Animation style:** GSAP + ScrollTrigger scroll-reveals site-wide (fade-up + stagger) — a different engine from T1's AOS; Typed.js hero word-cycle; Swiper (teacher circular carousel + testimonial fade-spotlight); GLightbox gallery; GSAP-driven animated number roll-ups (stats, no CountUp.js — avoids reusing T1's library for the same visual concept); CSS keyframe floating cloud/star shapes; vanilla-JS accordion (FAQ + reusable for future schedule use), tabs (age-group programs, URL-hash aware), teacher-bio modal, gallery category filter, FAQ live search; smart-scroll header (hides utility bar only, main row stays fixed).
- **Footer layout:** **Light** pastel sky-blue footer (not dark like T1) with a wave top divider, 3-column grid (brand+social+hours / quick links + programs sub-list / visit-us info + inline "Schedule a Tour" CTA button), centered bottom bar.
- **Mobile menu pattern:** **Full-screen fade+scale overlay** (fixed inset-0, **right-aligned** stacked links — `items-end` + `text-right`, so it flips to visually-left automatically under `dir="rtl"` — with staggered per-link transition-delay entrance), large top-right close button, "Home" renders as an inline accordion (chevron-button + collapsible Home 1/Home 2 sub-list) rather than a plain link, theme toggle (icon-only circle) + RTL/LTR toggle (icon + short "RTL"/"LTR" label, `whitespace-nowrap` + `shrink-0` so it can never wrap to two lines) sit in one non-wrapping row, CTA button at the bottom — completely different mechanic from T1's right-side slide-in drawer.
- **Special design features:** Dark/light mode toggle (localStorage), RTL/LTR toggle (localStorage), age-group program tabs shared between Home 1 and `programs.html` (URL-hash aware, `#infant` / `#toddler` / `#preschool` / `#prek`), interactive vertical daily-schedule timeline, admission enquiry + campus-tour-request form with child-DOB → auto age-group-suggestion logic and a dynamic `data-min-today` / `data-max-today` date-limit system (sets `min`/`max` via JS on load instead of a hardcoded date, so the form never goes stale), teacher-bio modal popups (click any teacher card site-wide — shared modal, no dedicated bio pages), gallery category filter + GLightbox (every `.glightbox` link needs an explicit `; type: image` in its `data-glightbox` attribute — see changelog), FAQ accordion with live text search, graceful `onerror` image fallback (inline gradient SVG data-URI) on every photo via a shared `ph-img` class + `initImageFallback()`, breadcrumb nav on every inner page, balloon-illustration 404 with floating decorative shapes, desktop "Home" mega-lite dropdown (2-item panel, hover + click + keyboard, closes on outside-click/Escape/focus-out).
- **Pages built:** index.html (Home 1), **home-2.html** (Home 2, added post-launch), about.html, programs.html, gallery.html, teachers.html, **pricing.html** (added post-launch), admissions.html (enquiry/tour form), faq.html, contact.html, 404.html.
- **Libraries used:** Tailwind CSS (CDN, class dark mode), Font Awesome 6 (icons), Google Fonts (Baloo 2 / Quicksand), GSAP 3.12 + ScrollTrigger (scroll reveals, replaces T1's AOS), Typed.js 2.1 (hero word-cycle, Home 1 only), Swiper 11 (teacher carousel + testimonial spotlight, Home 1 + Home 2 hero collage does **not** use Swiper), GLightbox (gallery lightbox). CountUp is done via a GSAP tween (`data-reveal-number`), not CountUp.js — used on Home 1's welcome-snapshot badge and Home 2's stats band. No build step; no backend.

**Do not reuse for future templates:** logo-left-centered-nav header with two-level utility bar and a hover/click "Home" mega-lite dropdown, coral/sky/sunshine/leaf multi-hue playful palette, Baloo 2 + Quicksand pairing, wavy-SVG-divider motif, layered dual blob-mask hero composition, objectBoundingBox SVG clip-path blob technique, sticker-card (thick-border + rotation + tape-pin) component, chunky flat-bottom-shadow "press" button mechanic, polaroid-filmstrip gallery preview, light pastel footer with wave-top, full-screen fade+scale mobile overlay menu (right-aligned), GSAP+ScrollTrigger as the site-wide reveal engine, vertical alternating dashed-rail timeline (Home 1 daily schedule), age-group tab switcher pattern, teacher-bio click-to-modal pattern, asymmetric photo-collage hero (Home 2), comparison-table "us vs typical daycare" section (Home 2), ticket-stub event cards (Home 1) / stacked date-badge event list (Home 2), horizontal dashed-line connected-circle process steps (Home 1) / sticker-card numbered process cards (Home 2), FAQ accordion preview (Home 1) / FAQ topic-icon grid (Home 2), pricing sticker-cards with a "Most Popular" ribbon + centered gradient final-CTA band (Pricing page).

### Home Page 1 (`index.html`) vs. Home Page 2 (`home-2.html`)
Added after initial launch, once the brief called for a second homepage. Same tokens (palette/type/header/footer/buttons/cards), deliberately different hero composition, section order, and component choices per page — no section on Home 2 reuses Home 1's exact layout for the same content type.

- **Hero layout:** Home 1 = layered organic blob-mask photo composition (image right, text left), Typed.js word-cycle headline, floating cloud/star shapes. Home 2 = **asymmetric photo collage grid** (one tall photo + two stacked smaller photos, plain rounded-2xl corners, no blob-mask), image **left**, text **right** (mirrored from Home 1), star-rating + licensing trust line instead of a word-cycle.
- **Section order (Home 2):** Header → Hero (photo collage) → **Quick Stats band** (dark full-bleed strip, 4 GSAP count-up numbers — new position for this device; Home 1 only uses a single count-up badge) → **Age Groups** (static 4-card photo grid with gradient-overlay hover-zoom links into `programs.html#...`, not the thabbed switcher Home 1 uses) → **Learning Programs / Curriculum Pillars** (4-icon band on a solid leaf-green confetti section — STEM / Literacy / Arts / Social-Emotional — a concept Home 1 doesn't have at all) → **Daily Activities** (horizontal scroll-snap photo strip with a time-of-day caption baked into each photo's gradient overlay, vs. Home 1's vertical dashed-rail timeline) → **Why Parents Choose Us** (2-column "Typical Daycare vs. Little Sprouts" comparison table with check/x rows, replacing Home 1's sticker-card icon grid entirely) → **Safety & Hygiene** (5-icon horizontal badge strip, no photo — vs. Home 1's photo+numbered-list split) → Meet Our Teachers (static 4-card grid linking to `teachers.html`, not a Swiper carousel) → Parent Testimonials (3-card grid, all visible at once, not a single-spotlight Swiper) → Upcoming Events (stacked list widget with square date chips + chevron, vs. Home 1's 3-card ticket-stub grid) → Enrolment Process (2×2 grid of sticker-cards with giant numerals, vs. Home 1's horizontal dashed-line connected circles) → FAQ (4-icon topic-card grid linking into `faq.html`, vs. Home 1's accordion preview) → Final CTA (full-bleed photo background with a dark gradient overlay and centered text, vs. Home 1's image-left/color-right split panel) → Footer (identical, shared component).
- **New nav entry:** "Home" became a dropdown (Home 1 / Home 2) in both the desktop nav and the mobile drawer, on all 11 pages.

### Pricing Page (`pricing.html`)
Added after initial launch as a dedicated main-nav page.

- **Hero:** New gradient combination — leaf-green→sky-blue diagonal gradient + confetti-dot pattern + a coin/leaf float-shape pair (not reused from any other inner-page hero gradient).
- **Section order:** Header → Hero → Pricing Plans (6 sticker-cards in a 3-col grid: Half-Day Daycare, Full-Day Preschool *"Most Popular"* with a ribbon badge + `-translate-y-3` lift, Full-Day Infant/Toddler, After-School Care, Optional Activities add-on, Drop-In/Hourly) → What's Included (6-item icon+text card grid) → Flexible Care Options + Sibling Discounts (2-column colored panels, leaf-green and coral) → Admission Process (numbered list beside a sticker-card CTA panel — lighter-weight than either homepage's process treatment) → Pricing FAQs (single-open accordion, 6 tuition-specific questions) → Final CTA (**centered gradient band**, sunshine→coral→coral, no photo, no split panel — a third distinct CTA treatment alongside Home 1's split-panel and Home 2's full-bleed-photo CTAs) → Footer.
- **Card style:** Reuses the site's `sticker-card` component (consistent with every other page) but adds a "Most Popular" ribbon + vertical lift for the featured plan — the only page-specific card variant, not a new component.

### Gallery Page redesign (`gallery.html`)
Redesigned after initial launch — replaced the original asymmetric "editorial" grid (one `col-span-2 row-span-2` featured tile + one `col-span-2 aspect-video` tile among plain squares) with a **uniform grid**: every tile is the same `aspect-[4/5]`, so the CSS grid auto-flow can never leave a gap or an incomplete row, in any filtered state, at any breakpoint. The asymmetric layout was the root cause of the original imbalance — with irregular tile spans, categories couldn't be filtered to an even, gap-free layout no matter how many photos each had.
- **Photo count:** Every category now holds exactly **4 photos** (16 total, was 4/3/3/2 unevenly). 4 new content-verified images were sourced via Unsplash search (not guessed IDs) to fill the gaps: one playground photo, one art-activity photo, two meal-time photos.
- **Filter tabs:** Each tab now shows a live photo count badge (`data-filter-count`, computed from the DOM at runtime, not hardcoded) — e.g. "Meals ⸱" — plus a category icon. Active tab styled via a new `.gallery-filter-btn[aria-selected="true"]` state (coral fill + bottom-shadow, distinct from the `tab-btn` class used for the age-group tabs elsewhere).
- **Category badge chips:** Each tile carries a small always-visible color-coded pill top-left (sky=Classroom, leaf=Playground, coral=Activity, sunshine=Meal Time) so the category reads at a glance even before hovering — new pattern, not used elsewhere in the template.
- **Load More pagination:** `data-gallery-grid` + `data-batch-size="8"` drive a JS batching system (`initGalleryFilter` in `main.js`, rewritten): "All Photos" shows 8 initially with a "Load More Photos" button revealing the rest; switching to any single-category filter shows all 4 of that category directly (no pagination needed for a set that small) and hides the Load More button; returning to "All Photos" resets the batch. A live `data-gallery-count` label ("Showing 8 of 16 photos") tracks the current state.
- **Reveal animation:** New `.gallery-tile` / `.gallery-tile-visible` CSS pair (opacity + translateY + scale transition) replaces the old instant `display:none` toggle — items entering view (via filter change or Load More) now fade/rise in smoothly instead of popping. Deliberately *not* wired through GSAP ScrollTrigger's `data-reveal` system, since ScrollTrigger initializes against an element's layout box at scan time and this grid's items are frequently `display:none` at load (everything past batch 1) — mixing the two would have produced unreliable trigger positions.

**Gallery page content expansion** (second pass, same file): the page went from "hero + filter grid + one-line CTA" to a full campus-life showcase. New section order after the grid: **Gallery Overview** (intro copy, added directly under the hero, plus a 4-icon stat strip — Photos / Classrooms / Playground / Years) → filter grid (existing) → **Classroom Highlights** (image-left split, floating stat badge, 2×2 checklist) → **Outdoor Play Area** (mirrored text-left/image-right split, inline stat trio instead of a checklist — deliberately not identical to Classroom Highlights) → **Arts & Crafts** (clustered, independently-rotated `.polaroid` pair standing in for a "gallery wall," text beside it) → **Learning Activities** (text-only 2×2 icon grid, no photos at all — an intentional pacing break between photo-heavy sections) → **Meals & Nutrition** (photo + 3-column "menu card" band: Breakfast/Lunch/Snacks) → **Parent Testimonials** (horizontal scroll-snap quote-card strip — a fourth distinct testimonial treatment alongside Home 1's spotlight swiper, Home 2's static 3-grid, and none of them reused) → **Gallery FAQ** (two independent `data-accordion="single"` groups side by side, so opening a question in one column never closes the other — distinct from every other page's single-column accordion) → **Enrolment CTA** (solid dark `bg-ink` band with a GSAP count-up stat trio + dual buttons — a fourth distinct CTA treatment alongside Home 1's split-panel, Home 2's full-bleed-photo, and Pricing's centered gradient).

### Programs, Teachers & About content expansion (third pass — `programs.html`, `teachers.html`, `about.html`)
All three pages went from a single lean section to a full, unique page — content only, no new page-level layout system (each already had a distinct header/hero treatment from initial launch).

- **`programs.html`:** added a "Curriculum Philosophy" intro band (3 icon pillars) right after the ratio strip. Each of the 4 existing age-group split sections gained a shared but per-program-colored sub-block below the checklist: a "A Typical Day" mini time-stamped schedule (4–5 entries) + a "Key Learning Outcomes" chip row (4 tags) — the same *template* repeated once per program is intentional here (users compare programs apples-to-apples), the *content* is unique per age group. Closed with a new "Which Program Is Right for Your Child?" comparison table (Age / Ratio / Hours / Focus × 4 programs) before the existing CTA. The Pre-K photo was replaced (see image-verification note below).
- **`teachers.html`:** added a "Teaching Methodology" 4-pillar icon band after the credentials strip. The flat 8-card teacher grid was reorganized into three labeled groups — **Leadership Team** (Director, Assistant Director) → **Classroom Teachers** (one lead per age group) → **Specialist Teachers** (Music, Art) — for real visual hierarchy instead of one undifferentiated grid. Every teacher card gained `data-years` / `data-qualification` / `data-specialties` / `data-certifications` / `data-quote`, and the shared teacher-bio modal (`#teacher-modal`, used here **and** on `index.html`'s swiper preview — both updated identically) was rebuilt to render a stat row (years + qualification, icon-led), specialty tags, a pull-quote blockquote, the existing bio paragraph, and a certifications row — `initTeacherModal()` in `main.js` populates all of it via a `fillTags()` helper and hides any row whose data attribute is absent, so cards that only set the original `data-name/role/img/bio` (there are none left, but the mechanism stays graceful) still render correctly. Added "Classrooms in Action" (3 teacher-name + signature-activity spotlight cards, e.g. "Ms. Carter's Story Circle"), a "Teacher Achievements & Growth" dark stat band (training hours, credentials earned, mentorship program, tenure — deliberately a *growth/development* angle, distinct from the top credentials strip's static aggregate numbers), and "What Parents Say About Our Teachers" (2×2 grid of quote cards paired with a colored initials-avatar and an explicit "on Ms./Mr. ___" teacher attribution — ties the testimonial content specifically to this page's people-first identity, and is the 5th distinct testimonial layout on the site alongside Home 1's spotlight swiper, Home 2's static 3-grid, Gallery's scroll-snap strip, and About's asymmetric masonry below).
- **`about.html`:** the largest expansion. New section order: Hero → Our Story (existing) → **Mission & Vision** (twin white cards, top-bordered coral/sky, side by side) → **Core Values** (existing 3-icon band, now with its own eyebrow+heading instead of sharing implied meaning with Mission) → **Our Milestones** (horizontal scroll-snap year-card timeline, 2011–2026 — a *company-history* timeline, deliberately distinct from Home 1's *time-of-day* daily-schedule timeline both conceptually and in orientation: horizontal cards here vs. vertical alternating rail there) → Impact Stats (existing dark band) → **Learning Approach** (image-left split + a numbered 3-step "how we teach" list, distinct from Home 2's 4-icon curriculum-pillar grid) → **Facilities** (asymmetric photo collage: one tall `row-span-2` classroom photo + a 2×2 grid of smaller facility photos, each with a gradient-overlay caption, linking to `gallery.html`) → Director spotlight (existing) → Accreditation & Safety (existing sticker-card grid) → **Awards & Recognition** (horizontal wrapped row of pill-shaped badges with a colored icon roundel — visually distinct from the accreditation grid directly above it on the same page) → **Community Involvement** (plain 3-column icon+text row, no cards/borders — the lightest-weight section on the page, a deliberate pacing break) → **Parent Testimonials** (asymmetric masonry: one large featured quote card + two smaller stacked cards beside it — the site's 6th distinct testimonial layout) → **FAQs** (static, non-interactive Q/A pairs with colored "Q"/"A" role avatars — deliberately *not* an accordion, since accordions are already used on `faq.html`, Home 1's preview, Pricing, and Gallery; this is the only FAQ presentation on the site with no click-to-expand interaction) → CTA (existing, now with an added inline trust-badge row above the button).

**Image-verification lesson applied immediately (see changelog item 9 below):** the Pre-K photo swapped into `programs.html` and the classroom photo added to `about.html`'s new Learning Approach section were both sourced via Unsplash-search-then-`WebFetch`-then-`Read` (not recalled from memory), and one already-mismatched image discovered *during this pass* was caught and replaced using the same technique.

### Header, mobile-menu & lightbox changelog (fixes applied after initial launch)
Three corrective revisions, recorded so future templates don't repeat the same mistakes:
1. **Split-nav → logo-left:** the original "centered-logo, split-nav-either-side" header was replaced with a conventional logo-left / centered-nav-links / right-controls layout, at explicit user request. Logo position is now identical on mobile and desktop (far left) rather than centering only on desktop and left-aligning only on mobile.
2. **Nav-link centering:** the centered nav-links group was initially left-aligned immediately after the logo (`flex-1` container, links packed to its start); changed to `justify-center` so the links center within the space between the logo and the right-side controls, per explicit request.
3. **GLightbox blank popup:** gallery images hosted on Unsplash have no file extension in the URL (`?auto=format&fit=crop&w=1400&q=80`), so GLightbox couldn't auto-detect them as images and rendered an empty iframe on click. Fix: every `.glightbox` anchor's `data-glightbox` attribute must include an explicit `; type: image` (e.g. `data-glightbox="title: Nursery Room; type: image"`) whenever the image URL lacks a recognizable extension.
4. **RTL skip-link scroll-width explosion:** the accessibility skip-link used the classic `left: -9999px` off-screen-hiding technique. Under `dir="rtl"`, the browser's overflow/scrollWidth calculation folds that huge negative offset into `document.scrollWidth` (observed: exactly `9999 + viewport-width`), which pushed the entire visible viewport off to a blank stretch of the inflated canvas — the whole page (not just the skip-link) appeared to render blank whenever RTL was toggled with the mobile menu open. Fix: hide the skip-link with `transform: translateY(-100%)` instead of a large negative `left` offset — no large offsets anywhere means nothing for RTL's overflow recalculation to inflate. **Lesson for future templates: never use `left: -9999px` (or any large fixed-pixel physical offset) for visually-hidden-until-focused elements on a template that supports RTL — use a transform-based hide instead.**
5. **Dropdown click-vs-hover race:** an early version of the desktop "Home" dropdown toggled open/closed on click while *also* opening on `mouseenter` / closing on `mouseleave`. A mouse user hovering (opens it) then clicking (toggle → closes it) got a dropdown that closed on click. Fix: click now always calls `open()` (never toggles), and closing is delegated entirely to `mouseleave`, outside-click, `focusout`, and Escape — click and hover no longer fight each other.
6. **Hero decorative shapes bleeding through the translucent header:** two of `index.html`'s floating cloud/star SVGs sat at `top-16`/`top-24` relative to the hero section — but since the header is `position: fixed` (removed from document flow), the hero section's own top edge is at document y=0, not below the header. At `lg`+ widths those shapes rendered underneath the header's `bg-white/95 backdrop-blur-md`, faintly bleeding through as a ghost artifact only visible on close inspection. Fix: pushed both shapes further down (`top-16`→`top-32`, `top-24`→`top-36`) to clear the header's ~92–104px height with margin. **Lesson: any absolutely-positioned decorative element inside the first section after a `position: fixed` header needs a `top` offset comfortably larger than the header's total height, since the section's own coordinate origin starts at the true document top, not visually below the header.**
7. **Mobile drawer content pinned to one edge at tablet widths:** per an explicit requirement the overlay itself must stay 100% width at every breakpoint, but the nav content inside it was `items-end`/`text-right` unconditionally — great on a 320–425px phone, but at 768–1279px (tablet, still below the `xl` hamburger cutoff) it left the content pinned to the far right of a mostly-empty full-width canvas. Fix: `items-end sm:items-center` + `text-right sm:text-center` (nav, the Home-submenu wrapper, and its panel) — right-aligned on phone widths, centered from `sm` (640px) up, while the overlay itself never stops being 100% width. Both requirements ("100% width" and "properly aligned on tablet") are satisfied simultaneously because they're about content alignment vs. container width — not the same property.
8. **`gallery.html` missing GSAP/ScrollTrigger includes:** the original gallery page never used scroll-reveal animation, so it never loaded the GSAP/ScrollTrigger `<script>` tags every other page includes. When later content sections were added using the site's standard `data-reveal` / `data-reveal-number` attributes, `initScrollReveals()` silently no-op'd (`typeof gsap === 'undefined'` guard) — the new sections just appeared statically (harmless) but the animated stat counters stayed frozen at their literal `0` fallback text forever (not harmless — a visibly broken counter). Fix: added the two missing script tags. **Lesson: `data-reveal*` attributes are inert markup, not a self-contained feature — always confirm the GSAP/ScrollTrigger scripts are actually present on a page before using them, especially when extending a page that predates the site's animation system.**
9. **Confirmed a genuine image/caption mismatch, and found a reliable way to catch more:** `about.html`'s new Learning Approach section used `photo-1497633762265-9d179a990aa6` captioned "Teacher guiding preschoolers through a hands-on classroom lesson" — an ID picked from memory very early in this project, before the site adopted Unsplash-search-then-verify sourcing. The rendered photo was actually a stack of colored books. Caught it by eye in a full-page review screenshot, not by any automated check (HTTP 200 + a WebFetch text description of the *search results page* both said nothing about the *actual photo's contents*). **Working technique discovered while fixing this: `WebFetch` can't describe an image URL directly (it only sees encoded binary and says so) — but it silently saves that binary to a local `.jpg` in the tool-results folder every time, and the `Read` tool *can* view that saved file. So `WebFetch` on the raw `images.unsplash.com/photo-…` URL (any prompt) → `Read` on the path it reports saving to, is a real way to visually verify a specific candidate image before committing to it, not just trust its alt-text search description.** Replaced with `photo-1761208662734-fb46f1398551`, verified this way before use. Apply this fetch-then-Read check to any image sourced from memory or from a search-results description rather than viewed directly — search descriptions describe the search snippet, not necessarily what's in the full-resolution photo.

10. **LTR mobile/tablet hamburger-left fix regressed RTL:** to move the hamburger to the left / logo to the right on mobile/tablet in LTR, the previous fix removed the hamburger's old unconditional `ml-auto` and replaced it with `ltr:order-1` / `ltr:order-2 ltr:ml-auto` (scoped only to `dir="ltr"`). This fixed LTR but left RTL with *no* edge-anchoring at all below `xl`: both elements defaulted to `order: 0` and sat packed together at the flex-start side (visually the right, since RTL mirrors `flex-direction: row`), leaving a large dead gap on the opposite side — exactly the bug in the reported screenshot. **Fix: added `justify-between` to the header's main flex row** (`<div class="flex items-center gap-4">` → `...justify-between gap-4`). This is direction-agnostic — `justify-content: space-between` always anchors the first and last flex-order items to the start/end edges, and "start/end" already flip correctly with `dir` on their own. Combined with the existing `ltr:order-1`/`ltr:order-2` swap (which only needs to override *which* element is first/last, not push anything to an edge), both directions now anchor correctly with no extra `ltr:`/`rtl:` duplication needed for the edge-pinning itself. At `xl`+ this has no effect, since the `flex-1` nav already consumes all free space regardless of `justify-content`. **Lesson: when reversing a two-element edge-pinned layout for only one text direction, prefer `justify-between` (inherently bidi-correct) over an `auto`-margin scoped to a single `dir:` variant — a scoped auto-margin fixes the direction you tested and silently strips the other direction's spacing if it was relying on that same margin before.**

### Lessons applied from Template 01's changelog
Two mistakes recorded in T1's header changelog were deliberately avoided here rather than re-learned: the centered-logo header used plain flex (`flex-1` siblings + `shrink-0` logo) from the very first draft instead of `grid-cols-3` equal-thirds, and desktop nav was gated at `xl` (1280px) from the start given a 6-link split nav needs at least as much room as T1's single-row 7-link version did.

### Content-accuracy note (image sourcing)
An initial pass populated photo placeholders with Unsplash `photo-<id>` URLs recalled from memory and verified only by HTTP 200 status — several turned out to depict unrelated subjects once rendered (a library interior captioned "Pre-K Classroom," a magnifying glass on a laptop captioned "Story Time," a street-photography shot in a gallery grid). HTTP 200 confirms a URL *resolves*, not that its *content* matches the caption. The fix: use `WebFetch` on Unsplash search-results pages (e.g. `unsplash.com/s/photos/preschool-classroom`) to extract real `images.unsplash.com/photo-…` URLs paired with their actual alt-text descriptions, then verify those specific URLs return 200 — only then treat an image as content-verified. Every photo in this template was re-sourced this way. Apply this method from the start on future templates rather than as a corrective pass.

### Cross-page image de-duplication pass
A full-site audit found the same photo reused across 2–5 *different* pages for entirely different sections — e.g. one "art studio" photo appeared on `index.html`, `home-2.html`, `about.html`, `gallery.html` (twice — once in the grid, once in its own Classroom Highlights section), and `teachers.html` simultaneously. Root cause: early sections were built by copying an existing card/section as a starting template and only the caption text was changed, not the image.
- **Policy adopted:** `gallery.html`'s filterable grid is the canonical home for every "facility scene" photo (classrooms, playground, art, meals, reading, etc.) since it's the dedicated photo-library page. `programs.html` is canonical for its own age-specific program photos. Every other page's use of the same photo was replaced with a fresh, independently-sourced one (search → verify with the `WebFetch`-then-`Read` technique above → confirm it doesn't collide with any of the ~50 IDs already on the site → apply).
- **Teacher/staff headshots are the deliberate exception** — the same person's photo is *supposed* to recur across their bio card, the shared teacher-modal, and any homepage preview swiper; that's correct identity consistency, not a duplication bug, and was left untouched.
- **When a caption no longer matched its new photo, the caption was rewritten, not left stale** — e.g. `about.html`'s Facilities collage had a "Dining Room" tile; the replacement photo (verified, unique, but not a meal scene) was captioned "Play & Discovery" instead of leaving a mismatched label. Swapping an `src` without checking the surrounding caption/label text is exactly the kind of thing that produces a *second* content-accuracy bug while fixing the first.
- **Scope of this pass:** fixed every photo that had been duplicated across 3 or more pages (the highest-severity groups: art, reading, nursery/classroom, dining, playground — each brought down to a single canonical page). A smaller set of 2-page duplicates was intentionally left for a follow-up pass given the sourcing effort involved (each replacement requires a search, a CDN-URL resolve, a collision check against every ID already on the site, and a visual fetch-then-Read verification): the "prek classroom" photo (`1777056491418`, on `index.html` + `programs.html`, gallery.html kept as canonical), "music/movement" (`1779398422654`, on `home-2.html` + `teachers.html`), "healthy snacks" (`1777056481869`, on `home-2.html`), "play kitchen" (`1763310225537`, on `home-2.html`), "toddler room" (`1601339434203`, on `home-2.html`), and the `index.html` age-tab panel images that mirror `programs.html`'s infant/toddler/Pre-K photos (`1560307815`, `1558576997`, `1709301264789`) — `programs.html` was treated as canonical owner for those three since it's the more detailed, dedicated page. The Infant tab photo was later swapped twice more per follow-up requests (final: `1593793373220`, a baby on a colorful sensory play mat) — each swap re-verified via the fetch-then-Read technique and re-checked against every ID on the site before landing.

### Horizontal scroll strips unusable with a desktop mouse
Several sections (`index.html`'s facility gallery preview, `home-2.html`'s daily-activities strip, `about.html`'s milestones timeline, `gallery.html`'s testimonial strip) use `overflow-x-auto no-scrollbar` — deliberately hiding the scrollbar for a cleaner look. This left desktop mouse users with **no way to reach the overflow content at all**: a mouse only produces a vertical wheel delta, which just scrolls the *page* past the section, and there's no visible scrollbar to drag since it's hidden by design. Touch and trackpad users never noticed since horizontal swipe gestures work natively. Confirmed via a Playwright test *before* fixing: `scrollWidth` (1424px) genuinely exceeded `clientWidth` (1280px) — extra content existed and was permanently unreachable by mouse.
- **Fix, centralized in `main.js`/`style.css` rather than per-page** (`initHorizontalScrollStrips()`, applied via the generic `.overflow-x-auto.no-scrollbar` selector so it automatically covers every current and future strip using this pattern, no per-page wiring needed): a `wheel` listener redirects vertical wheel delta into `scrollLeft` and calls `preventDefault()` so the page itself stops scrolling while hovering a strip with room left to scroll; a `mousedown`/`mousemove`/`mouseup` set adds click-and-drag scrolling (`cursor: grab` → `grabbing` via a `.is-dragging` class, which also sets `pointer-events: none` on children mid-drag so a card's own hover/link states don't fight the drag); a `moved` flag suppresses the trailing `click` event after an actual drag so dragging a strip never accidentally activates a link/card underneath the cursor, while a plain (non-drag) click still navigates normally.
- **Lesson: `overflow-x-auto` + `no-scrollbar` is a mobile-first pattern that silently fails for desktop mouse users** — if a horizontal-scroll strip needs a hidden scrollbar for visual cleanliness, it needs the wheel-redirect + drag-to-scroll pair (or explicit prev/next buttons) from the start, not just on mobile where native swipe already works. Apply `initHorizontalScrollStrips()`'s selector-based approach on future templates too, since it fixes every matching strip site-wide with zero per-instance markup changes.

### `subjects.html` toolbar layout fix + subject-count rebalance (fourth pass, post-redesign-3)

Two follow-up fixes applied after the third full redesign (toolbar + horizontal list-cards, 6-per-page pagination) documented above, in response to specific user feedback rather than a new visual concept.

**Toolbar layout fix ("subject tab not perfect"):** the redesign's filter toolbar used a two-group `flex flex-col lg:flex-row lg:items-center gap-5` structure — "All Subjects" as one item, then two `flex-1 min-w-0` sibling containers (Grade Band / Subject Area) each with their own block-level label and an internal `flex-wrap` pill row, separated by `hidden lg:block w-px h-9` divider bars. Playwright screenshots at 375/768/1024/1280/1440px plus computed-style checks surfaced three real bugs:
1. At `lg` (1024px) specifically, the two `flex-1` siblings split the row into **equal** halves regardless of actual content — Grade Band (3 short pills) left a large dead gap on its right, while Subject Area (6 pills) got squeezed into the remaining equal half and wrapped to 2 rows unnecessarily. Resolved itself at 1280px purely because there was enough absolute width, not because the layout was correct.
2. The vertical divider bars were visually imperceptible in every screenshot — not functioning as separators.
3. Below `lg` (flex-column mode), "All Subjects" stretched to the full container width (confirmed via `getBoundingClientRect()`: button width == parent width) because flex-column children default to `align-items: stretch` and the button had no width constraint — it rendered as a large rectangular banner instead of a compact pill, inconsistent with the filter pills stacked below it.

**Fix:** replaced the two-group structure with a single flowing `flex flex-wrap items-center gap-2` pill row containing "All Subjects" + all 9 filter pills together, with small inline `text-[0.65rem]` "Grade:" / "Subject:" labels flowing in-row (not block-level headers) and small non-`flex-1` `<span>` divider bars that just wrap along with everything else. This eliminates the equal-width-split bug entirely (no more `flex-1` siblings to split unevenly), fixes the "All Subjects stretches" bug (flex-wrap items don't stretch — only flex-column items with default `align-items: stretch` do), and the dividers now render reliably since they're no longer sized against an unpredictable equal-split context. Re-verified via Playwright at all 5 breakpoints (no full-width "All Subjects" pill at any width, zero horizontal overflow, row count decreases smoothly from 6 rows at 375px to 3 rows at 1024px+), plus a full functional re-test (filtering, pagination page-size-6, dark mode, RTL) — zero console errors, all `data-filter`/`filter-count` wiring unchanged since the fix was purely structural/CSS, no JS changes needed.
- **Lesson: `flex-1` on sibling containers gives them equal width shares regardless of how much content each actually holds** — fine when the groups are naturally similar in size, but a toolbar with a 3-pill group next to a 6-pill group needs either an intrinsic-sized layout (flex-wrap, no `flex-1`) or explicit unequal `flex-basis` values, never a bare `flex-1` split. A single flowing row sidesteps the whole problem since nothing needs to be pre-allocated a share.

**Subject-count rebalance ("subject card atleast have 2 or 4 only"):** the Subject Area filter counts were Math 3, Science 3, English 4, Social Studies 3, Coding 3, Languages 2 — four subjects sat at 3, which the user wanted eliminated in favor of exactly 2 or 4 per subject. Rather than adding or deleting cards (which would have broken the 18-card total and the clean 6/6/6 grade-band split that pagination and prior verification depend on), two existing cards were **relabeled with genuinely new, grade-appropriate content**:
- Middle School's "Coding with Python" (`data-filter-subject="coding"`) became **"Geometry & Data Analysis"** (`data-filter-subject="math"`) — geometric proofs, graph/data-set reading, intro probability & statistics. A real, distinct middle-school math elective, not a duplicate of the existing "Pre-Algebra & Algebra I" card.
- High School's "World History & Government" (`data-filter-subject="social-studies"`) became **"Environmental Science & Ecology"** (`data-filter-subject="science"`) — ecosystem dynamics, climate/sustainability case studies, field-data analysis. A real, distinct high-school science elective, not a duplicate of "Biology, Chemistry & Physics".

Both new cards reuse the icon already established for their new subject (math → `fa-square-root-variable`, science → `fa-flask`, matching every sibling card of that subject) rather than inventing a one-off icon, keeping the "one icon per subject, consistent across grades" convention intact. Net result: Math 4, Science 4, English 4 (unchanged), Social Studies 2, Coding 2, Languages 2 (unchanged) — every subject is now exactly 2 or 4. Total cards, grade-band counts (6/6/6), and pagination all stayed unchanged since only the `data-filter-subject` value and content of two existing cards moved, nothing was added or removed. Toolbar `filter-count` badges (static text, not computed at runtime on this page) were hand-updated to match. Verified via Playwright: DOM attribute counts, live badge-vs-actual-visible-count match for all 6 subjects, grade bands still 6/6/6, zero console errors.
- **Lesson: when a numeric-distribution constraint conflicts with an existing structural constraint (card total / grade-band balance), relabeling existing cards with authentic new content is preferable to adding or removing cards** — it satisfies the new constraint without disturbing anything the earlier, already-verified constraints depend on. This is consistent with the standing preference for real, thematically-justified content over tag-gaming: the fix didn't just retag a card while leaving its title mismatched, it rewrote the card's identity (title, description, bullets, trial-CTA text) to genuinely match its new subject.

### Site-wide Back to Top button + Font Awesome favicon (fifth pass, all 12 pages)

Two small site-wide additions, applied identically across every page (`index.html`, `home-2.html`, `about.html`, `subjects.html`, `tutors.html`, `pricing.html`, `faq.html`, `trial-class.html`, `contact.html`, `login.html`, `dashboard.html`, `404.html`).

- **Favicon:** the site had no favicon at all. Rather than a raster icon, built a self-contained `assets/favicon.svg` — a 64×64 rounded-square in brand primary blue (`#2F63B8`, matching the header's "B" logo badge) with the official Font Awesome 6 solid `graduation-cap` glyph (fetched as raw SVG path data from the Font Awesome GitHub repo, not approximated from memory, to guarantee a correct render) centered in white. `<link rel="icon" type="image/svg+xml" href="assets/favicon.svg">` added to every page's `<head>`, immediately after the shared stylesheet link. SVG favicons need no separate light/dark variant handling and scale cleanly at any tab size; broad modern-browser support (Chrome/Firefox/Edge) was judged sufficient for this project's scope.
- **Back to Top button:** a `data-back-to-top` circular button (`fixed`, `bottom-5 right-4` on mobile → `sm:bottom-8 sm:right-6` on larger screens, `rtl:right-auto rtl:left-4 sm:rtl:left-6` to mirror correctly under RTL, `w-11 h-11` mobile → `sm:w-12 sm:h-12` desktop) was added to every page directly before its `<script src="assets/js/main.js">` tag — same insertion point across all 12 files, applied via a small one-off Node script (`fs.readFileSync`/`replace`/`writeFileSync`, not 24 manual edits) since the insertion points were byte-identical in every file. A new `initBackToTop()` in `main.js` toggles `opacity-0`/`opacity-100` + `pointer-events-none`/`pointer-events-auto` + `translate-y-3`/`translate-y-0` on scroll (`window.scrollY > 400` threshold, passive scroll listener) and calls `window.scrollTo({top:0, behavior:'smooth'})` on click. Deliberately did **not** combine the show/hide `translate-y-*` utility with a `hover:-translate-y-*` lift effect on the same button, since both would target the same CSS `transform` property and fight for cascade priority — used `hover:scale-105` instead, a different property, for the tactile hover cue.
- **Verified via Playwright** across 10 representative pages: favicon `<link>` resolves on every page, button starts hidden (`opacity:0`, `pointer-events:none`) on load, fades in after scrolling past 400px, click-to-scroll lands at `scrollY === 0`, RTL correctly repositions it to the bottom-left (measured 24px from the left edge at `sm`+ widths, matching `sm:left-6`), and mobile viewport (375px) renders it at the expected `44×44px` (`w-11`/`h-11`) size pinned to the bottom-right with no overlap with page content. Short pages with no scrollable overflow (`login.html`, `404.html`) correctly keep the button hidden throughout, since `scrollY` never exceeds the 400px threshold — expected behavior, not a bug. Zero console errors and zero failed favicon requests across every page tested.

### `.folder-card` tab icon visibility fix (sixth pass, site-wide via shared CSS)

The `folder-card` signature component (used for testimonials, pricing plan cards, home-page pricing preview, subjects.html's trial-diagnostic card, and every other folder-tab card across all 12 pages) renders a small colored ribbon/tab-notch above the card via a `::before` pseudo-element, with a Font Awesome icon (`.folder-tab-icon`) layered on top. The icon was reported as not clearly visible — confirmed by screenshotting the pricing-preview and testimonial sections in both themes: the icon was `font-size: 0.85rem` (~13.6px), a bare `<i>` positioned by eyeballed `top`/`left` offsets with no centering box, inside a 54×22px notch. It was legible but small and off-center, and had materially weaker contrast on the `#E8A33D` secondary/gold tab variant (white-on-light-gold) than on the `#2F63B8` primary/blue variant.

**Fix, in `style.css`'s `.folder-card` block only (no HTML changes needed anywhere — every page consumes the same shared class):**
- Enlarged the tab notch (`::before`) from 54×22px to 60×26px (hover state 26px→30px, scaled proportionally) and added a subtle resting `box-shadow` so the ribbon itself reads with a touch more depth.
- Converted `.folder-tab-icon` from a bare absolutely-positioned `<i>` into a `32×26px` `display:flex; align-items:center; justify-content:center` box, so the glyph is pixel-perfect centered inside the notch regardless of which Font Awesome icon it is (checked `fa-clock`, `fa-star`, `fa-boxes-stacked`, `fa-quote-left`, `fa-circle-question`, `fa-users`) — no more eyeballed offsets.
- Bumped icon `font-size` from `0.85rem` to `1.05rem` (~16.8px) for real legibility at a glance, not just on close inspection.
- Added `text-shadow: 0 1px 3px rgba(0,0,0,0.5)` (light mode) / `0 1px 4px rgba(0,0,0,0.7)` (dark mode, via `.dark .folder-card .folder-tab-icon`) — since the site only ever uses two `--tab-color` values (`#2F63B8` blue, `#E8A33D` gold, confirmed by grepping every `--tab-color:` occurrence across all 12 pages), a single shadow-based contrast boost works universally instead of needing per-color-variant icon-color logic. This is what fixed the specific "hard to see on gold" complaint — the shadow anchors the white glyph against either background.
- Added a hover micro-interaction (`transform: translateY(2px) scale(1.08)` on the icon) so the tab now visibly acknowledges hover, consolidating the two previously-duplicated `.folder-card:hover::before` rules (one at the original definition, one added later in the "glow-shadow" section) into one.
- RTL mirroring (`[dir="rtl"] .folder-card .folder-tab-icon`) was already in place and needed no changes — kept `left`/`right` swapped consistently with the notch's own RTL clip-path flip.

**Verified via Playwright** with `[data-reveal]` opacity/transform/transition forced off (so scroll-reveal mid-fade frames don't muddy the screenshot) across index.html's testimonials grid (6 cards, 2 tab colors) and pricing preview, subjects.html's trial-diagnostic card, and pricing.html's 4 plan cards (4 tab colors including the "Most Popular" ribbon variant) — in light mode, dark mode, and RTL+dark combined, plus a hover-state screenshot to confirm the lift/scale doesn't clip or misalign. Icons read clearly at a glance in every combination tested; zero console errors across 9 pages after the change.
- **Lesson: a component-level fix in shared CSS (one `.folder-card` block) propagates correctly to every consumer across all 12 pages with zero HTML edits, as long as the component doesn't have per-page copy-pasted style overrides** — worth checking `grep`-wide for a component's custom-property usage (here, every `--tab-color` value site-wide) before fixing, to confirm a single fix covers every real-world variant rather than just the one instance that prompted the report.

---

## Template 03 — Local Grocery Store

- **Brand name:** GreenCart Local Market
- **Tagline:** "Fresh Groceries, Local Roots."
- **Business category:** Local/neighborhood grocery store (produce, dairy, bakery, pantry, beverages, household — no full e-commerce checkout, no dashboard)
- **Primary color:** Basil Green #16653E / #2E8F57 (500) — CTAs, brand
- **Secondary color:** Heirloom Tomato #E8542A (500) — discount ribbons, sale accents
- **Accent:** Golden Wheat #E09E33 — star ratings, badge highlights, footer top divider
- **Neutral / background:** Kraft cream #FBF8F2 (light), Forest charcoal #0F1712–#1B281F (dark) — deliberately not brown/choco (T1) or navy/indigo (T2)
- **Font combination:** Space Grotesk (geometric grotesk, headings/display) + Inter (body) — a 2-font pairing, but structurally distinct from T2's Baloo 2 + Quicksand (both rounded): Space Grotesk is angular/technical, giving a "clean, practical" retail feel per the brief rather than playful or ornate.
- **Header type:** **Three-tier header**, a new structural pattern for this site: (1) a non-sticky top strip combining a CSS `@keyframes` marquee ticker (scrolling offers/announcements, pauses on hover, disabled under `prefers-reduced-motion`) with live store-hours/phone info — scrolls away with the page, not fixed; (2) a smart-scroll (hide-on-scroll-down/show-on-scroll-up) main row: logo-left, centered nav (`flex-1`, gated at `xl` per T1/T2's shared lesson), a **click-to-expand inline search overlay** that swaps the nav row for a full-width search input + live-filtered results dropdown (not a separate page or modal), theme/RTL toggles, a "My List" icon with a localStorage item-count badge, hamburger; (3) a **sticky secondary category-chip bar** (Produce/Dairy/Bakery/Pantry/Beverages/Household/Offers) with IntersectionObserver scroll-spy active-states — this bar *is* the site's primary product navigation (grocery categories matter more than page links for this vertical), distinct from both T1's single unified row and T2's two-level utility-bar-plus-nav.
- **Navigation style:** Centered nav group (Home dropdown: Home 1/Home 2, About, Categories, Offers, Contact, FAQ) + the category chip bar described above as a second, parallel navigation system. Contact and Offers stay directly visible per the master prompt's rule.
- **Hero layout (Home 1):** Split layout — left: eyebrow badge, headline, inline quick-search bar (not just a CTA), dual buttons, three inline stat pill-badges (icon + count-up number, no vertical dividers — deliberately not T1 Home 2's stat-trio-with-dividers pattern). Right: **2×2 photo collage grid** (not a single full-bleed photo, not a blob-mask) with a rotated "Save up to 25%" sticker badge and a floating "Today's Pick" product-spotlight card (image+name+price+add-to-list) overlapping the bottom-left corner — a commerce widget, not a stats counter (T1's floating card reserved for CountUp stats).
- **Hero layout (Home 2):** Full-bleed photo banner + dark gradient overlay, centered text, a prominent white search bar embedded directly in the hero, and a **quick-category "dial"** — a rounded card of 5 circular category icons overlapping the hero's bottom edge — the site's third distinct hero mechanic.
- **Hero layout (inner pages):** Compact banners (h-56 to h-72) with breadcrumb, each using a different verified photo (aisle gondola, checkout counter, supermarket aisle, produce display) with a dark gradient overlay — no repeated crop/treatment across pages.
- **Section order (Home 1):** Header (3-tier) → Hero (collage) → Trust/USP icon strip → Shop by Category (6 uniform image cards) → This Week's Offers (countdown timer + Swiper carousel of price-tag cards) → Fresh Produce Spotlight (horizontal scroll-snap strip) → Why Shop With Us (image+stat-badge / checklist split) → Store Hours & Location (Google Maps iframe embed + live-updating hours table + address card) → Testimonials (static 3-card grid, star ratings — no carousel) → Newsletter + Bulk-Order promo (2-panel split) → Footer.
- **Section order (Home 2):** Header → Hero (full-bleed + category dial) → Today's Highlights pill band (static, not a marquee — distinct from the header's marquee) → Featured Categories (asymmetric bento grid: 1 tall + 2×2) → Deal of the Day (single spotlight card with its own countdown, not a multi-card carousel) → Browse by Aisle (numbered "01–06" horizontal store-directory cards, `aisle-card` component with a bidi-aware colored border) → Meet Our Partner Farmers (3-card bios, dark section) → Bulk & Business Orders promo (checklist + image split) → Store Info compact 3-column strip → Testimonials (Swiper single-spotlight fade — the site's second distinct testimonial treatment) → Final CTA (centered gradient band) → Footer.
- **Card style:** `tag-card` — rounded-xl, thin 1px border, a CSS `clip-path` **die-cut price-tag notch** on one top corner with a small circular "hole" punched into it (mirrors for RTL), flat `translateY(-6px)` + shadow lift on hover (no glow, no rotation, no thick border — mechanically distinct from T1's scalloped `cake-card` and T2's rotated/tape-pinned `sticker-card`), diagonal `tag-ribbon` discount banner for sale items.
- **Button style:** `btn-fresh` — solid rounded-lg (not pill, not chunky-flat-shadow) fill; on hover the background darkens and a trailing icon slides via `translateX` (mirrors for RTL, `rotate(180deg)` on the arrow glyph). Outline variant uses a `::before` pseudo-element that wipes in as a solid fill from the leading edge on hover (`scaleX`, `transform-origin` flips for RTL) — a third distinct button mechanic alongside T1's gradient lift+glow and T2's flat-bottom-shadow press.
- **Image style:** 2×2 photo-collage hero (Home 1), full-bleed + overlapping category-dial hero (Home 2), uniform image category cards with gradient overlay + hover-zoom, horizontal scroll-snap produce/aisle/farmer strips, price-tag notch product cards. No masonry gallery, no polaroid frames, no blob-masks, no flip-cards, no lightbox (kept deliberately light for a "clean, practical" grocery site per the brief).
- **Background style:** Kraft-cream base with a subtle fine-line **graph-paper/dot-grid texture** (`texture-grid`, linear-gradient hairlines) — a third distinct light-mode texture concept, structurally different from T1's radial sprinkle-dot pattern and T2's confetti-dot pattern (lines vs. dots in both cases). Dark mode uses solid forest-charcoal panels, no texture.
- **Animation style:** **Vanilla IntersectionObserver** scroll-reveal (`data-reveal` / `data-reveal="left|right|scale"`, no external library) — a third distinct reveal engine alongside T1's AOS and T2's GSAP+ScrollTrigger; CSS `@keyframes` marquee ticker (bidi-aware, reduced-motion safe); a hand-rolled `requestAnimationFrame` count-up for stats (not CountUp.js, not a GSAP tween — a third distinct counter technique); a real-time weekly-offers countdown timer (`setInterval`, resets to the coming Sunday); a live "Open Now / Closed" store-status indicator computed from the actual client clock against a stored weekly-hours table; Swiper 11 used narrowly (offers carousel + Home 2 testimonial fade only — not the site's primary animation engine). `initHorizontalScrollStrips()` (wheel-redirect + drag-to-scroll for mouse users) reused from the T1/T2 lesson on every `.scroll-strip.no-scrollbar` instance site-wide.
- **Footer layout:** Dark forest-green, **straight-edge top divider** (a repeating gold/tomato diagonal-stripe bar, `.footer-divider`) — deliberately not scalloped (T1) or wave-curved (T2). 4-column grid: brand+live-status-badge+social / shop categories / customer care / store info+directions button. Bottom bar: copyright + payment-method icons.
- **Mobile menu pattern:** **Bottom-sheet drawer** — slides up from the bottom (`translateY`, rounded-t-3xl, drag-handle bar), containing search input, nav links (Home as a native `<details>` accordion, no JS needed), a 3×2 category-icon grid, theme/RTL toggle row, and a bulk-order CTA. A fourth distinct mobile-menu mechanic alongside T1's right slide-in drawer and T2's full-screen fade+scale overlay.
- **Special design features:** Dark/light mode toggle (localStorage), RTL/LTR toggle (localStorage), live open/closed status badge (real-time clock vs. a JS weekly-hours table, reused in the top strip, Store Hours section, Home 2's info strip, and the footer), real-time countdown timers (offers banner + Home 2's Deal of the Day, both resolving to the same coming-Sunday deadline), sticky category chip-bar with IntersectionObserver scroll-spy (shared between the header nav and `categories.html`'s own in-page anchors), click-to-expand header search overlay with a small client-side product index and live-filtered results dropdown, quantity steppers + a persisted "My List" badge count (`localStorage`, `data-add-to-list` / `data-stepper`) used across the offers carousel and every category product card, a **dynamic Bulk & Special Order form** (`contact.html#bulk-order`) with add/remove item rows, per-row quantity + a live estimated-subtotal calculator, and a `min=tomorrow` delivery-date field set via JS on load; a Google Maps `output=embed` iframe (no API key) on both Home 1 and Contact; a general contact form with full custom validation (name/email/phone/message + live character counter); an FAQ page with five independent single-open accordion groups by topic; graceful `onerror` image fallback (`ph-img` + `initImageFallback()`, same technique as T2) on every photo; a grocery-themed 404 ("This aisle is empty," cart icon + question-mark badge, live product/page search box).
- **Pages built:** index.html (Home 1), home-2.html (Home 2), about.html, categories.html (full 6-aisle product directory with steppers/add-to-list), offers.html (countdown + category-filterable discount grid), contact.html (map + hours + general form + bulk-order form), faq.html, 404.html.
- **Libraries used:** Tailwind CSS (CDN, custom `[data-theme="dark"]` selector strategy), Font Awesome 6 (icons), Google Fonts (Space Grotesk + Inter), Swiper 11 (offers carousel on Home 1 + testimonial fade on Home 2 only — no AOS, no GSAP, no GLightbox, no Typed.js, no CountUp.js). No build step; no backend.

**Do not reuse for future templates:** three-tier header (marquee-info strip + smart-scroll main row + sticky scroll-spy category-chip bar), click-to-expand inline header search overlay with a live results dropdown, basil-green/tomato/gold/kraft-cream palette, Space Grotesk + Inter pairing, 2×2 photo-collage hero with a floating product-spotlight card (Home 1), full-bleed hero + overlapping circular category-dial (Home 2), fine-line graph-paper background texture, price-tag die-cut notch `tag-card` component, wipe-fill `btn-outline` mechanic + slide-arrow `btn-fresh` mechanic, bottom-sheet mobile menu, straight-edge diagonal-stripe footer divider, vanilla-IntersectionObserver scroll-reveal as the site-wide animation engine, hand-rolled `requestAnimationFrame` count-up technique, live open/closed store-status pattern, numbered "01–06" `aisle-card` store-directory strip (Home 2), asymmetric bento featured-categories grid (Home 2), single-spotlight Deal-of-the-Day card with its own countdown (Home 2), dynamic multi-row bulk-order form with live subtotal + JS-set `min=tomorrow` date field.

### Image sourcing & verification (applied from the start, per T1/T2's registry lessons)
Every photo was sourced via `WebFetch` on Unsplash search-results pages to get real `images.unsplash.com/photo-…` URLs paired with actual alt-text, not recalled from memory. A representative sample of the most prominent images (hero collage tiles, storefront/interior banners, team photos) was further verified using the fetch-then-Read technique documented in T2's registry (`WebFetch` the raw CDN URL — it silently saves the binary to a `tool-results/*.jpg` file even though it can't describe it — then `Read` that saved file to see the actual photo before committing to it). All spot-checked images matched their captions.
- **Caught and fixed during this pass, not after:** two storefront/exterior photos initially chosen for the About and Contact page heroes turned out to prominently display a *different*, real business's signage ("Riverside Market," "Mercadona") baked into the photo. Since the brand in this template is "GreenCart Market," a hero photo with a competing store name reads as a visible content-accuracy defect, not just a stylistic mismatch. Both were swapped — About's hero to an unbranded aisle/produce-display photo, Contact's hero to an unbranded checkout-counter photo — before the pages were ever marked complete. **Lesson for future templates: real estate/storefront stock photography frequently has another business's actual name or logo legible in the shot — treat any photo with readable third-party signage as unusable for a hero/banner on a fictional-brand page, the same way a caption/content mismatch would be treated, and check for it specifically since it won't show up in a simple "does this photo depict a grocery store" content check.**
- **Cross-page reuse policy:** most photos are used on exactly one page; a small set of category-representative photos (e.g. the produce-market wide shot, the aisle-interior shots) intentionally recur across 2 pages at most (e.g. once as a Home 1 hero-collage tile and once as a `categories.html` product/category image) where the same subject legitimately belongs in both places — consistent with T2's registry precedent that 2-page reuse of a genuinely on-topic photo is acceptable while 3+ page reuse is not.

### Testing & functional-bug changelog (found and fixed via headless-Chrome + Puppeteer interaction testing before sign-off)
Unlike T1/T2 (whose changelogs record fixes made after user reports), this template was interaction-tested end-to-end — dark/light toggle, RTL/LTR toggle, header search, mobile bottom-sheet, category filters, steppers, all three forms, FAQ accordion, and horizontal-overflow checks at 320/375/425/768/1024/1280/1440px on every page — **before** being marked complete, using headless Chrome (`--headless=new --screenshot`) for visual review and a Puppeteer-core driver (pointed at the system Chrome install) for scripted interaction. Three real bugs were caught this way and are recorded here so future templates can watch for the same classes of mistake:
1. **`data-reveal="left"/"right"` scroll-reveal transforms caused permanent horizontal overflow on every page.** The reveal system's initial (pre-intersection) state applies `transform: translateX(±32px)` so elements slide into place. Since this transform is applied to *every* matching element on page load — including ones far below the fold — any element whose untransformed position was already near the left/right edge of the viewport pushed the document's scrollable area past the viewport width from the very first paint, before any scrolling or JS observer had fired. This produced a consistent 8–19px `scrollWidth > clientWidth` overflow at *every* breakpoint on *every* page, confirmed via `document.documentElement.scrollWidth - clientWidth` in a scripted check. **Fix: `overflow-x: hidden` on `html, body`** — this is the standard, defensive counter-measure for exactly this class of bug (any decorative/animation transform that might transiently or permanently extend layout bounds) and was verified to bring overflow to exactly 0px at all 7 tested widths across all 8 pages afterward. **Lesson: any template using transform-based entrance animations (translateX/Y reveal, floating decorative shapes, etc.) needs a global `overflow-x: hidden` safety net, not just careful placement of individual elements — CSS transforms on far-below-fold elements can silently extend `document.scrollWidth` before the user ever scrolls there or the animation resolves.**
2. **Header search overlay had no results container in the markup.** The JS (`initHeaderSearch` → `renderResults`) was written against a `[data-search-results]` selector that was never actually added to the header HTML on any page — the search input and close button existed, but typing produced no visible output at all (a silently-dead feature, not a crash). Caught by a scripted test that typed "milk" and asserted at least one result rendered; it returned zero. **Fix:** added an absolutely-positioned `[data-search-results]` dropdown panel (rounded, bordered, scrollable) as a sibling of the input inside the now-`relative` search-overlay container, on all 7 pages that include the header search. **Lesson: when JS is written against a `data-*` hook, grep the HTML to confirm that hook actually exists before considering the feature done — a missing container fails silently (no console error) rather than loudly.**
3. **Success messages were nested inside the `<form>` they belonged to, so hiding the form on submit also hid its own success message.** The submit handlers in `main.js` (`initForms`, `initBulkOrderForm`) add a `hidden` class to the `<form>` element itself and simultaneously try to reveal a `[data-form-success]` message — but on the general contact form and the bulk-order form, that success `<p>` was a *child* of the form being hidden, so `display: none` on the parent made the success message unconditionally invisible regardless of its own class state. (The two newsletter forms were unaffected — their success message happened to already be written as a sibling *after* `</form>`, which is what exposed the inconsistency.) Caught by a scripted test that submitted valid data and asserted the success message's computed visibility, not just its class list. **Fix:** restructured both affected forms so `[data-form-success]` is a sibling of `<form>` (wrapped in a shared parent `<div>` where needed to preserve grid layout), and updated `main.js` to look up the success element via `form.parentElement.querySelector('[data-form-success]')` instead of `form.querySelector(...)`. **Lesson: never place a "success" confirmation message as a descendant of the form element you plan to hide on success — verify the fix by checking actual rendered visibility (e.g. `!el.classList.contains('hidden')` *and* the element is reachable), not just that the class-toggle code ran without throwing.**

### Premium redesign pass (Home, Categories, Offers, Contact, FAQ)
After initial launch, the client asked for a substantially more premium/ThemeForest-quality treatment of five pages, explicitly rejecting "simple card-only" layouts. Rather than a new template, this was a *design-system extension* of T3: new shared components were added to `style.css`/`main.js` and reused across all five pages, keeping the established palette/type/header/footer/RTL/dark-mode system intact.

**New shared components added (available site-wide, not just on the redesigned pages):**
- `.bento-card` — image tile with gradient overlay, hover lift + zoom + reveal-description panel, rotating arrow button (bidi-aware). Used for premium category grids (Home bento scroll strip, Categories' 8-card showcase, Offers' featured-deals bento).
- `.blob` decorative gradient-blur shapes + `.texture-grid` — layered behind hero sections for depth, replacing flat single-color hero backgrounds on Categories/Contact/FAQ.
- `.promo-banner` (radial-gradient + dot-pattern overlay) — used for Offers' hero and FAQ's support-CTA band; visually distinct from the plain `bg-primary-dark` solid sections used elsewhere.
- `.coupon-ticket` / `.copy-code-btn` — a dashed-perforation "ticket" component with a punched-hole notch and a working copy-to-clipboard button (`data-copy-code`, `initCopyCode()` in `main.js`) — new interaction pattern, distinct from the existing `tag-card` price-tag notch.
- `.quote-premium` / `.avatar-ring` — testimonial cards with a large background quote-mark glyph and a gradient-ring avatar frame, replacing the site's original plain-bordered testimonial cards on Home.
- `.field-float` — floating-label form fields (label animates up/shrinks on focus or filled state) for the newsletter and general-contact/bulk-order static fields; deliberately *not* applied to the bulk-order form's dynamically-JS-generated item rows or the native `<input type="date">` field (floating labels don't play well with native date-picker placeholder rendering).
- `.accordion-premium` — icon-in-a-box accordion trigger (rotates 180° + inverts to filled color on expand) used on FAQ and Contact's FAQ-preview, replacing the plain-bordered accordion rows from the original build.
- `.faq-search-wrap` + `initFaqSearch()` — live client-side FAQ search across every `[data-faq-item]`, with a result-count readout and a "no results" empty state; matching items inside `data-accordion="single"` groups auto-expand while non-matches auto-collapse.
- `data-tilt` + `initTiltCards()` — lightweight pointer-driven 3D tilt on hover, gated to `(pointer: fine)` and `prefers-reduced-motion: no-preference` so it never activates on touch devices or for motion-sensitive users.
- `data-word-cycle` + `initWordCycle()` — vanilla cross-fade word rotator for the Home hero headline (`Local Roots. / Real Flavor. / Farm Direct. / Picked This Morning.`), avoiding a new external dependency (no Typed.js, keeping with T3's vanilla-first animation policy).
- `.logo-marquee-track` — a second marquee instance (trust/certification badges under the Home hero) reusing the existing marquee keyframes but as a distinct content lane from the header's offers ticker.
- `.ribbon-corner` — a CSS-triangle corner ribbon (bidi-aware) for the seasonal-offer banner on Offers, distinct from the diagonal `tag-ribbon` used on product cards.

**Page-level changes:**
- **Home (`index.html`):** hero rebuilt with blob backdrop + word-cycle headline + tilt photo collage + a trust/certification marquee strip beneath it (new). "Shop by Category" changed from a uniform 6-card grid to a horizontally-scrollable bento strip with badges (Best Seller / Daily / New). "This Week's Offers" wrapped in a `.promo-banner` with a coupon-ticket teaser pair added beneath the existing carousel. Testimonials upgraded from plain cards to `.quote-premium`. Newsletter field upgraded to `.field-float`.
- **Categories (`categories.html`):** added a new premium hero section — an 8-tile bento showcase (6 real aisles + "This Week's Bestsellers" linking to Offers + "Meet Our Local Farms" linking to About) sits above the existing, unchanged 6-aisle priced product directory (kept for real functionality: steppers, add-to-list, chip-bar anchors). A `.divider-glow` + heading separates "the full directory" from the showcase so both purposes read clearly on one page.
- **Offers (`offers.html`):** fully rebuilt per spec — `.promo-banner` hero with countdown, a "Featured Offers" asymmetric bento (Deal of the Week + two stacked cards), a dark "Flash Deals" horizontal strip, a 4-card coupon-code grid, a seasonal "Autumn Harvest Sale" ribbon-corner banner, the existing filterable "All Discounts" grid (kept, restyled with `data-tilt`), and a final `.promo-banner` newsletter CTA. This page does not load Swiper (never did) — the new "featured/flash" carousels use the existing `.scroll-strip` drag/wheel mechanism instead, kept consistent with the rest of the site.
- **Contact (`contact.html`):** hero rebuilt with blob backdrop + 3 quick-contact `info-card-premium` tiles (Call / Email / Bulk Orders), a new trust-indicator strip, hours/location cards upgraded to `.info-card-premium`, general-contact fields upgraded to `.field-float`, and a new FAQ-preview section (4 `.accordion-premium` items in a `data-accordion="multi"` group) added before the footer. The bulk-order form's structure, IDs and `data-*` hooks were preserved exactly (only its static top-level fields got `.field-float`; the dynamically-generated item rows keep their original plain-bordered inputs since they're built as raw HTML strings in `main.js` and retrofitting floating labels there wasn't worth the risk to the working add/remove/subtotal logic).
- **FAQ (`faq.html`):** fully rebuilt — blob-backdrop hero with a live search bar, a "Popular Questions" quick-jump grid (anchor links into the categorized list below), the same 5 topic groups as before but restyled as `.accordion-premium` inside `data-faq-group` wrappers (required for the new search feature to scope/filter correctly), a "no results" empty state, and a `.promo-banner` support CTA at the end.

**Scope decision — category taxonomy:** the brief's "only 8 category cards, not all categories" was interpreted as *presentation*, not a request to expand the store's real product taxonomy. Expanding to 8 literal grocery aisles would have required touching the chip-bar, footer links, mobile-sheet aisle grid, and search index on all 8 pages, plus building two entirely new priced product sections. Instead, Categories' new 8-tile showcase uses the existing 6 real aisles plus two editorial tiles ("This Week's Bestsellers" → `offers.html`, "Meet Our Local Farms" → `about.html`) — satisfying the "8 cards, premium, badged, not a plain grid" requirement without fragmenting the site's information architecture that every other page already depends on.

**Bugs caught during this pass (scripted-test + visual-review, before sign-off):**
1. **FAQ search had a one-way "ratchet" bug that permanently mis-expanded accordion items.** `page.type()`-style character-by-character input fires an `input` event per keystroke; the original handler set `aria-expanded="true"` on any item matching the *current partial* query but never set it back to `false` when a later, more specific keystroke stopped matching it. Typing "delivery" letter-by-letter (d → de → del → …) transiently matched nearly every FAQ item (most contain the letter "d" somewhere) and force-expanded all of them; by the time the full word landed only 3 items were *visible*, but all ~13 remained `aria-expanded="true"` underneath — confirmed by a scripted check (`TRUE_AFTER_DELIVERY_SEARCH: 13` before the fix). **Fix:** the expand/collapse line changed from `if (match && q) { …aria-expanded, 'true' }` (write-only) to `if (q) { …aria-expanded, match ? 'true' : 'false' }` (always reconciles both directions whenever a query is active). **Lesson: any "auto-expand on search match" feature that fires per-keystroke must also explicitly *un*-expand non-matches on every keystroke, not just expand matches — otherwise state from an earlier, broader partial-query accumulates and never clears.**
2. **One Unsplash photo ID (`1682092016074-b136e1acb26e`, a farmer-in-field shot) started 404ing** — used on both `home-2.html`'s "Halden Family Farm" card and the new Categories 8-tile showcase's "Meet Our Local Farms" tile. Caught visually (a bento tile rendering as bare alt-text on a blank background instead of a photo) rather than by the earlier link-check pass, since the URL had returned 200 at original sourcing time and only broke later. **Fix:** replaced both usages with a freshly-sourced, fetch-then-Read-verified photo (`1777113317559-fb3e17e8b60c`, farmers bagging harvested potatoes). While investigating, a full sweep of all 48 unique image URLs on the site was run (`xargs -P 10 curl -o /dev/null -w '%{http_code}'`) confirming this was the only broken one. **Lesson: Unsplash CDN URLs are not permanently guaranteed to stay live — a photo verified at sourcing time can 404 later (removed by photographer, moderation, etc.) — so a template that's been sitting for a while (or is being revisited for a redesign pass) is worth a cheap bulk HTTP-status sweep of every image URL, not just a one-time check when each image was first added.**
3. **A "premium" (`plus.unsplash.com`) photo candidate had a tiled "Unsplash+" watermark baked into the image itself**, only visible by actually viewing the rendered photo (HTTP 200, normal file size, no error) — the fetch-then-Read technique caught it while sourcing a replacement for bug #2 above, before it was ever committed to a page. Not all `premium_photo-*` IDs are watermarked (four *other* `plus.unsplash.com` photos already used elsewhere on the site were re-verified in the same pass and are clean), so the `premium_photo-` naming prefix alone is not a reliable signal either way. **Lesson: treat every `plus.unsplash.com` candidate as requiring visual (fetch-then-Read) verification before use, specifically checking for a repeated diagonal watermark — HTTP 200 and a plausible file size are not sufficient signals that a premium-tier photo is actually free to render cleanly.**

---

## Template 04 — Tutoring Center (School Subjects)

- **Brand name:** Brightpath Tutoring Center
- **Tagline:** "Guided Learning, Real Progress."
- **Business category:** K-12 tutoring center (Primary Grades 1–5, Middle School 6–8, High School 9–12) covering Math, Science, English, Social Studies, Computer Science/Coding and foreign languages (Spanish/French) — 1:1 and small-group tutoring, in-person and online, plus test prep. Includes a demo parent/student portal with a protected dashboard (a first for this template series — T1/T2/T3 have no login/dashboard).
- **Primary color:** Scholar Blue #2F63B8 (500), full 50–900 ramp from #EEF3FC to #14264A
- **Secondary color:** Marigold #E8A33D (500), ramp 50–700 from #FDF6E8 to #A3671F
- **Neutral / background:** Paper #FBF9F5 (light base), card white #FFFFFF, ink text #1F2430; dark mode "ink-slate" 900 #12151C (page bg) / 800 #1B2029 (cards) / 700 #262C38 (borders), text rendered via Tailwind's slate-100/300/400 for contrast rather than a bespoke dark-text token
- **Font combination:** Lora (serif, headings/display) + Manrope (sans, body/UI/nav) — a 2-font pairing, structurally distinct from T1's triple (Fraunces+Caveat+Poppins), T2's rounded pair (Baloo 2+Quicksand) and T3's geometric pair (Space Grotesk+Inter)
- **Header type:** **Floating detached navbar** — a new structural pattern for this site. At scroll-top, a full-width edge-to-edge transparent-to-page-background bar (logo + hamburger/controls only, minimal height). Past an 80px scroll threshold, JS toggles the header shell's container classes so it detaches into a centered, `max-w-6xl mx-auto` **rounded-2xl floating pill/card** with visible page-background gutters on both sides, elevated shadow and `mt-3` margin — distinct from T1's permanent full-width glass bar, T2's two-level utility-strip-plus-main-row, and T3's three-tier marquee+smart-scroll+chip-bar.
- **Navigation style:** Single flex row, logo (shrink-0) → centered nav group (`flex-1 justify-center`, gated at `xl`/1280px per the T1/T2/T3 shared lesson) → right cluster (shrink-0: search-icon toggle, theme toggle, RTL toggle, ghost Login link, solid "Book Trial Class" CTA, hamburger below `xl`). 7 direct links: Home, About, **Subjects** (hover/click dropdown → Primary/Middle/High grade-band anchors on `subjects.html`, using the T2-documented click-always-opens/mouseleave-or-outside-click-or-Escape-closes fix from the start), Tutors, Pricing, FAQ, Contact.
- **Hero layout (Home):** **Tilted flashcard-stack composition** — left: eyebrow badge, Lora serif headline, subtext, dual CTA ("Book a Free Trial Class" / "Explore Subjects"), inline row of 6 subject icon chips, trust line. Right: a large `rotate(-2deg)` rounded photo card with marigold corner-bracket frame accents, overlapped by two independently-tilted mini-cards (a "Next Class · Algebra II · Today 4:30 PM" schedule snippet, and a 5-star parent-quote snippet) — a fourth distinct hero mechanic alongside T1's full-bleed+blob+stats-card, T2's dual blob-mask layered photos, and T3's 2×2 collage/full-bleed+category-dial pair.
- **Section order (Home):** Header → Hero (tilted flashcard stack) → Stats strip (4 vanilla counters: Students Enrolled, Expert Tutors, Subjects Covered, Average Grade Improvement) → **Subjects by Grade Level** (vertical expandable accordion list, 3 grade-band rows, each revealing a horizontal scroll-strip of folder-tab subject cards — deliberately not a tab-switcher, which is T2's reserved pattern) → **Meet Our Tutors preview** (horizontal scroll-snap strip of folder-tab tutor cards, wheel-redirect + drag-to-scroll from the start, click opens the shared bio modal) → **How Trial Classes Work** (diagonal step-ladder: 4 numbered steps ascending along a staggered `sm:ml-[20/40/60%]` diagonal, a new process geometry vs. T1's horizontal icon row/zig-zag and T2's vertical dashed-rail/horizontal dashed-circles) → **Why Parents Choose Brightpath** (image-left / benefit-checklist-right split) → **Testimonials** (staggered 3-column masonry of quote cards, all visible, no carousel) → **Pricing preview** (3 folder-cards, middle "Most Popular" with a lift) → **FAQ preview** (4-question single-open accordion + "View All FAQs") → **CTA banner** (torn-notebook-paper dashed card) → Footer.
- **Card style:** `folder-card` — white/ink-800 `rounded-b-xl` cards with a small colored trapezoidal tab (CSS `clip-path: polygon(...)`, mirrors to the opposite corner under `dir="rtl"`) sticking up from the top corner, holding a small icon; tab color set per-card via a `--tab-color` custom property; soft shadow, `translateY(-6px)` lift + tab-color darken on hover — no rotation, no thick border, no tape/pin, no die-cut notch (T1/T2/T3's respective signatures).
- **Button style:** `.btn-write` / `.btn-write-outline` — solid/outline `rounded-md` (moderate radius, not pill, not chunky-flat-shadow, not slide/wipe) fill in Scholar Blue, with a leading pencil/arrow icon that plays a short `rotate(-10deg)→rotate(10deg)→rotate(0)` **wiggle keyframe** on hover/focus — a fourth distinct button mechanic alongside T1's gradient lift+glow, T2's flat-bottom-shadow press, and T3's slide-arrow/wipe-fill pair.
- **Image style:** Tilted flashcard hero photo with corner brackets, folder-tab tutor/subject cards, a custom vanilla-JS lightbox mini-gallery (About page learning-space photos) — no masonry gallery, no blob-masks, no flip-cards, no GLightbox.
- **Background style:** Paper base (`#FBF9F5`) with a subtle **ruled-notebook-line texture** (`repeating-linear-gradient` horizontal hairlines, low opacity) reserved for the hero and footer only — not a site-wide texture, and a third distinct light-mode texture concept alongside T1's radial sprinkle-dot and T2's confetti-dot (T3's was a graph-paper grid).
- **Animation style:** **Vanilla IntersectionObserver** scroll-reveal (`data-reveal="fade-up|fade-left|fade-right|zoom-in"`, `prefers-reduced-motion` respected by skipping transitions entirely) as the sole site-wide reveal engine — a fourth distinct engine alongside T1's AOS, T2's GSAP+ScrollTrigger and T3's vanilla-IO-without-a-shared-name; a **custom vanilla count-up** module (`data-counter`, IntersectionObserver-triggered `requestAnimationFrame`, eased) — a fourth distinct counter technique alongside T1's CountUp.js, T2's GSAP tween and T3's hand-rolled rAF counter (same technique family as T3 but implemented independently for this codebase); `initHorizontalScrollStrips()` (wheel-redirect + drag-to-scroll, ported forward as a standing lesson from T1–T3) on every `.scroll-strip` instance; a minimal custom vanilla-JS photo lightbox (About page gallery) — no GLightbox, no Swiper, no Typed.js, no CountUp.js.
- **Footer layout:** Dark ink-900, **torn-paper zigzag top edge** (`clip-path: polygon(...)` hand-authored jagged pattern, absolutely-positioned strip overlapping the section above) — deliberately not T1's scalloped bumps, T2's smooth wave curve, or T3's straight diagonal-stripe divider. 4-column grid: brand+social / quick links / get-started links (trial class, contact, portal login) / contact info. Bottom bar: copyright + tagline.
- **Mobile menu pattern:** **Top slide-down panel** — docks directly under the floating header bar (`position:absolute; top:100%` inside a `position:relative` header shell so it never affects document flow), `translateY(-130%)` closed → `translateY(0)` open, `rounded-2xl` matching the header, centered stacked nav list, Subjects rendered as a native `<details>` accordion (no JS needed for that part), theme+RTL toggle row, Login + Book Trial Class buttons — a fourth distinct mobile-menu mechanic alongside T1's right slide-in drawer, T2's full-screen fade+scale overlay, and T3's bottom-sheet.
- **Special design features:** Dark/light mode toggle (localStorage, respects `prefers-color-scheme` on first visit only), RTL/LTR toggle (localStorage, `dir` attribute on `<html>`, Tailwind's native `rtl:`/`ltr:` variants confirmed working against the `dir`-on-ancestor selector without any extra plugin), shared tutor bio modal (populated from `data-*` attributes, reused identically on Home's preview strip and the full `tutors.html` directory, focus-trapped, Escape/outside-click/close-button dismissal), client-side tutor filter (`tutors.html`, single active filter across subject **and** grade-band tokens via substring match on `data-filter-subject`/`data-filter-grade`), expandable grade-band accordion (Home), diagonal step-ladder process, custom vanilla lightbox (About), trial-class booking form with full validation (required fields, email regex, phone pattern via `data-phone`, `min` date set via JS on load — never hardcoded, select-required checks) and a styled success panel that is a **sibling** of the `<form>` it replaces (not a descendant — avoids the T3-documented "hiding the form also hides its own success message" bug from the start), general contact form with a distinct purpose dropdown + live character counter, FAQ page with 5 categorized sections (General/Enrollment/Payments/Trial Class/Online Learning) plus a live text-filter search that reconciles `aria-expanded` in both directions on every keystroke (the T3-documented one-way "ratchet" bug avoided from the start), **localStorage demo-auth dashboard pattern**: `login.html` checks a single hardcoded demo account (`parent@brightpath-demo.com` / `Demo@123`), sets `bp_auth`+`bp_parent_name` in `localStorage` and redirects to `dashboard.html`; `dashboard.html` runs an inline guard `<script>` at the very top of `<body>` that calls `window.location.replace('login.html')` before any content paints if `bp_auth` is absent (no flash-of-protected-content); a collapsible-on-mobile sidebar app-shell (deliberately distinct chrome from every marketing page) with anchor-scroll sections and IntersectionObserver-driven active-link highlighting (chosen specifically to avoid a second click-to-swap tab-panel pattern, since that's T2's reserved territory); a two-child **student switcher** (Aarav, Grade 8 / Ananya, Grade 5) that re-renders Overview/Classes/Attendance/Homework/Tests from a hardcoded `DASHBOARD_DATA` object; a custom-built SVG ring gauge for attendance percentage (no chart library); homework "mark as done" checkboxes persisted to `localStorage` under a per-student key so switching students shows correct completion state; test countdowns computed from `new Date()` at render time (never hardcoded, so they never go stale); Logout clears `bp_auth` and redirects. On-brand 404 page with a subject-search box and Home/Subjects/Contact links.
- **Pages built:** index.html (Home), about.html, subjects.html, tutors.html, trial-class.html, pricing.html, faq.html, contact.html, login.html, dashboard.html, 404.html.
- **Libraries used:** Tailwind CSS (CDN, class dark-mode strategy), Font Awesome 6 (icons), Google Fonts (Lora/Manrope). No AOS, no GSAP, no Swiper, no Typed.js, no CountUp.js, no GLightbox — every interactive/animation feature (scroll-reveal, counters, modal, lightbox, dropdown, accordion, horizontal-scroll drag) is hand-rolled vanilla JS in `assets/js/main.js`, with dashboard-only mock data and rendering logic kept separate in `assets/js/dashboard.js`. No build step; no backend.

**Do not reuse for future templates:** floating detached navbar (transparent full-width top state → centered rounded-2xl floating pill on scroll), Scholar Blue/Marigold/paper/ink-slate palette, Lora + Manrope pairing, `folder-card` tab-notch component, `btn-write` rounded-md icon-wiggle button mechanic, torn-paper zigzag footer top edge (and its matching dashed torn-notebook CTA card), diagonal step-ladder process layout, tilted flashcard-stack hero composition (rotated photo card + two independently-tilted overlapping mini-cards), top slide-down mobile menu panel docked under a floating header, ruled-notebook-line hero/footer texture, vanilla-IntersectionObserver-plus-hand-rolled-counter as this template's specific named animation stack, shared `data-*`-populated tutor bio modal pattern, and the **localStorage demo-auth protected-dashboard pattern** (inline pre-paint redirect guard, single hardcoded demo account, two-student switcher re-rendering from a hardcoded data object, per-student localStorage-persisted homework checkboxes, custom SVG ring gauge) — this is the first template in the series with a login/dashboard at all, so the whole mechanism is reserved.

### Revision pass — Home 2, richer visual system, Subjects & Tutors redesign, search removal
Added after initial launch, in one combined pass. Covers: a "Home" dropdown (Home 1 / Home 2) replacing the old single Home link on all 9 marketing pages; a new `home-2.html`; a site-wide richer visual system layered onto every inner-page hero; a full redesign of `subjects.html` and `tutors.html`; and confirmation that the header's old inline search feature (an earlier, since-abandoned experiment) has zero remaining references anywhere in the codebase.

**Home Page 1 (`index.html`) vs. Home Page 2 (`home-2.html`)**
Same tokens (palette/type/header/footer/buttons/cards), deliberately different hero mechanic, section order and component choices — no section on Home 2 reuses Home 1's exact layout for the same content type, per the standing "Home 1 vs Home 2 must not share section layouts" rule already applied on Templates 01–03.
- **Hero layout:** Home 1 keeps its original tilted flashcard-stack composition (rotated photo card + two overlapping mini-cards, text left). Home 2 is a **full-bleed background photo** (boy at a desk in front of a chalkboard full of equations, dark navy gradient wash) with a **glass `Quick Match` panel** (`.glass-card`, `backdrop-filter: blur`) floated on top — a 3-field mini-form (Subject / Grade Band / Format) that submits via `initQuickMatch()` in `main.js` straight to `trial-class.html` with the picks pre-filled as query-string params (decorative, no backend). This is a structurally new hero mechanic for the template (T4 previously only had one hero style since it launched without a Home 2).
- **Section order (Home 2):** Header → Hero (glass Quick Match over full-bleed photo) → **Trust marquee** (infinite-scroll strip of licensing/rating badges, `.marquee-wrap`/`.marquee-track`, pause-on-hover, wheel/drag not needed since it auto-scrolls) → **Results bento grid** (asymmetric tile grid — one large `col-span-2 row-span-2` stat tile + four smaller tiles, mixing `section-gradient-deep`, solid secondary, and plain card tiles) → **Explore by Subject bento grid** (6 subject tiles, one large `col-span-2 row-span-2` Math tile + five smaller tiles, links into `subjects.html#primary/#middle/#high`) → **Top Tutors Spotlight** (static 4-card grid, not a scroll-strip — deliberately different mechanic from Home 1's horizontal scroll-snap tutor strip; cards reuse the shared `data-tutor-card`/tutor-modal system and carry the new rating/availability/methods data) → **How Quick Match Works** (radial hub-and-spoke process — a center "Quick Match" hub node with 4 spoke-connected step cards at desktop, `.hub-center`/`.hub-spoke-line`, falls back to a plain stacked list below `lg` — a new process geometry alongside Home 1's diagonal step-ladder) → **Comparison table** ("Brightpath vs. Typical Tutoring," 6-row check/x table) → **Spotlight testimonial with avatar switcher** (`data-testimonial-widget`/`data-testimonial-trigger`/`data-testimonial-panel`, a new lightweight vanilla-JS module — `initTestimonialSwitcher()` in `main.js`, no new library) → **Pricing teaser band** (single wide folder-card with inline price trio + one CTA, vs. Home 1's 3-card pricing grid) → **Final CTA** (full-bleed graduation photo with dark gradient overlay, centered text — vs. Home 1's torn-notebook CTA card) → Footer (identical, shared component).
- **New nav entry:** "Home" became a dropdown (Home 1 / Home 2) in both the desktop nav and the mobile `<details>` accordion, on all 9 marketing pages (the 8 pre-existing pages plus `home-2.html` itself); the dropdown trigger gets `nav-link-active` and the current page's entry is visually bolded inside the panel on both home pages.
- **Images:** Home 2's hero (`photo-1758685733395`, boy at a chalkboard) and final-CTA (`photo-1541339907198`, graduation cap toss) photos are both new, content-verified via the fetch-then-Read technique (see below) and not reused anywhere else on the site.

**Richer visual system (site-wide, `style.css` / `main.js`)**
Layered on top of the original design system — nothing from the original launch was removed, only added to:
- `.blob` — organic blurred decorative shape (radial-gradient fill, `blob-primary`/`blob-secondary`/`blob-white` color variants, gentle CSS keyframe float, `prefers-reduced-motion` disables the animation). Used as corner accents on every hero across all 9 pages.
- **Gradient hero treatment** (`hero-photo-layer` + `hero-gradient-overlay` + `hero-grad-1` through `hero-grad-7`) — replaces the old flat `bg-primary-800` solid-color inner-page hero with a low-opacity photo layer, a tasteful gradient wash (7 distinct angle/color-stop variants so no two inner pages look identical), and the existing `texture-notebook` hairline pattern on top. Applied to `about.html`, `subjects.html`, `tutors.html`, `trial-class.html`, `pricing.html`, `faq.html` and `contact.html`, each with its own unique, content-verified background photo (none reused elsewhere on the site). `index.html`'s hero (already photo-based, not flat color) got two `.blob` accents added for visual-system consistency rather than a full rework.
- **Glow-shadow on `folder-card` hover** — the existing hover box-shadow rule was supplemented (not replaced) with a second, later rule using `color-mix(in srgb, var(--tab-color) 32%, transparent)` so the shadow now tints toward each card's own tab color. Deliberately layered as an *additional* rule after the original static-rgba hover shadow rather than editing it in place — in browsers without `color-mix()` support, the invalid declaration is dropped during parsing and the original shadow silently remains as a working fallback; no `@supports` block needed.
- `.btn-write-gradient` — optional gradient variant of the existing `.btn-write` button (Scholar Blue → Marigold diagonal fill), used for the higher-emphasis CTAs on Home 2 and the Tutors mid-page CTA band.
- `[data-tilt]` — subtle pointer-driven 3D tilt (`initTilt()` in `main.js`, CSS custom properties `--tilt-x`/`--tilt-y`, capped at 6°, skipped entirely on touch devices and under `prefers-reduced-motion`). Applied to the Tutors directory cards and Home 2's Top Tutors Spotlight cards.
- `.marquee-wrap`/`.marquee-track` — CSS-only infinite horizontal scroll (mask-fade edges, pause on hover, disabled under `prefers-reduced-motion`), used for Home 2's trust-badge strip.
- `.glass-card`/`.glass-input` — frosted-glass panel (`backdrop-filter: blur`) with light/dark-aware opaque form-input variants, used for Home 2's hero "Quick Match" panel.
- `.bento-tile`, `.section-gradient-soft`, `.section-gradient-deep`, `.hub-center`/`.hub-spoke-line` — supporting pieces for Home 2's bento grids and radial process section, plus a couple of tasteful gradient section-background variants (used sparingly: Tutors' stat band and mid-page CTA, Home 2's bento tiles — not applied as a blanket site-wide texture).

**`subjects.html` redesign**
- New richer hero (`hero-grad-2`, open-book photo, blobs) replacing the old flat `bg-primary-800` banner.
- New **intro band** (4-column "how to read this page" strip) directly under the hero.
- New **real interactive filter bar** — `data-subject-filter-group` + `initSubjectFilter()` in `main.js`, mirroring `initTutorFilter()`'s pattern (single active filter, substring match against `data-filter-subject`/`data-filter-grade`), plus additional logic specific to this page: each of the three grade-band `<section>` wrappers carries `data-grade-row="primary|middle|high"` and is hidden entirely when none of its cards match the active filter, with a "no subjects match" empty state (`data-subject-no-results`). **Implementation note:** the row wrapper's band attribute is intentionally named `data-grade-row` rather than `data-grade-band` — an early version used `data-grade-band` on both the row *and* every card inside it (for per-card bucketing), and because `document.querySelectorAll("[data-grade-band]")` matched both, the row-visibility pass re-processed each card as if it were a row and silently un-hid everything the card-filter pass had just hidden. Caught via an automated Playwright check (filtering to "Coding" left all 19 cards visible instead of 3) before this shipped. **Lesson: never reuse the same `data-*` attribute name for two different element roles that a single non-scoped `querySelectorAll` call might both match** — scope the selector or use distinct attribute names, even when the values happen to overlap semantically.
- Every subject card gained a "what you'll learn" bullet list (3 bullets), a format tag pair (`1:1 or Small Group` / `Online & In-Person`), and a per-card "Book a Trial in [Subject]" CTA link — previously cards were description-only.
- Existing "How We Teach Each Subject" section and closing CTA kept as-is.

**`tutors.html` redesign**
- New richer hero (`hero-grad-3`, tutor-team photo, blobs).
- New **"Meet the Team" stat band** (`section-gradient-deep`, 4 `data-counter` stats: certified tutors, average rating, subject areas, average years experience) directly under the hero.
- Every tutor card (on `tutors.html`, plus the 6-card preview strip on `index.html` and the new 4-card spotlight grid on `home-2.html`) gained `data-rating`, `data-availability` and `data-methods` attributes, a visible star-rating line, an availability line and 2 method chips on the card face, and the shared `#tutor-modal` (duplicated identically across `index.html`, `tutors.html` and `home-2.html`) gained matching `rating`/`rating-num`/`availability`/`methods` fields — `initTutorModal()`'s `open()` function now also renders a 5-star icon row from `data-rating` and fills the two new rows (which hide gracefully via the existing `data-modal-row` mechanism if a card ever omits them).
- New **"Our Teaching Methods"** section (6-card icon grid describing team-wide teaching principles, distinct from the per-tutor method tags on individual cards).
- New **mid-page CTA band** ("Most Tutors Have Openings This Week," `section-gradient-deep` + `.btn-write-gradient`) inserted between "Our Teaching Methods" and the pre-existing closing torn-notebook CTA, so the page now has two CTAs with a full content section between them rather than two back-to-back.

**Search-feature removal — confirmed complete**
Re-verified via `grep -rniE "data-search-toggle|initHeaderSearch|search-wrap"` across every `.html` file and `main.js`: zero matches. The header search icon/toggle feature from an earlier iteration is fully gone and was not reintroduced anywhere in this pass.

**Image sourcing for this pass**
All 9 new photos (About/Subjects/Tutors/Trial-Class/Pricing/FAQ/Contact hero photos, Home 2's hero photo, Home 2's final-CTA photo) were sourced via Unsplash search → candidate `images.unsplash.com/photo-…` URL extraction → the established `WebFetch`-then-`Read` visual verification technique (WebFetch can't describe a fetched image directly, but it saves the binary locally and `Read` can view that saved file) before any was committed to markup, per the standing content-accuracy lesson from earlier templates. Every candidate ID was also checked against the full list of IDs already in use on the site to guarantee no unintentional duplicate (tutor headshots remain the one deliberate, policy-documented exception, reused across `index.html`/`tutors.html`/`home-2.html` for the same people).

**Verification pass**
An automated Playwright sweep (Chromium, viewport widths 320/768/1440px) was run across all 9 marketing pages, checking: `document.documentElement.scrollWidth` vs `clientWidth` (no horizontal overflow at any width, before or after toggling dark mode and RTL), zero `console.error`/`pageerror` events during load or interaction, and functional correctness of the new interactive pieces (subjects filter — including the `data-grade-row` bug above, caught and fixed by this same sweep; tutors filter + modal rendering the new rating/availability/methods fields; Home 2's Quick Match form redirecting to `trial-class.html` with the expected query string; Home 2's testimonial avatar switcher swapping panels correctly). All internal `href="*.html"` targets were cross-checked against the files that actually exist on disk (none dangling). All checks passed after the one fix above.

### Post-launch fix: `.btn-write-gradient` missing from shared button base rule
`.btn-write-gradient` (introduced in the richer-visual-system pass above) was defined as a standalone rule setting only `background`/`color`/`border` — it was never added to the shared `.btn-write, .btn-write-outline { display:inline-flex; align-items:center; justify-content:center; gap; border-radius; padding; font-weight; transition }` base rule, nor to the `:active` / `.btn-icon` / wiggle-keyframe selector lists. Wherever it sat next to a `.btn-write-outline` sibling (Home 2's final CTA, a CTA on `tutors.html`), it rendered as an unpadded, non-flex inline chunk of text — visibly misaligned and non-responsive next to the properly-styled outline button. Fixed by adding `.btn-write-gradient` to all four shared selector lists in `style.css` so it now inherits identical sizing/spacing/hover/press/wiggle behavior. **Lesson: when adding a new `.btn-write-*` variant, it must be added to every one of the base rule's shared selector lists, not just given its own color override — a variant that only overrides color/background silently loses all layout properties if the base rule doesn't already target it via a shared selector.**

### Post-launch content pass: Home 2 background photos + Home 1 hero/grade-section redesign
Four additional revisions, done directly (not via a background build agent) since the scope was bounded to specific sections:

- **`home-2.html` "What Families Actually See" (Results bento grid):** the featured 2×2 tile changed from a flat `.section-gradient-deep` fill to a background photo (a tutor teaching a young student, `photo-1771765767087-ce71e4a7916a`) + dark gradient overlay, keeping the white stat/caption text legible; a `.blob-secondary` decorative shape was added to the section for depth. The four smaller stat tiles were left as-is (flat white/secondary) so the grid doesn't overload.
- **`home-2.html` "Explore by Subject" (bento grid):** the featured 2×2 Math tile and the Science and Coding tiles changed from flat color fills to background-photo + gradient-overlay tiles (`photo-1453733190371-0a9bedd82893` chalkboard equations, `photo-1758685734030-a31d96462eec` a student-and-instructor chemistry experiment, `photo-1597933471507-1ca5765185d8` a boy at a laptop) — English/Social Studies/Languages stayed icon-only white tiles, giving the grid a deliberate 50/50 photo/icon rhythm rather than photo-ifying every tile. A `.blob-primary` shape was added to the section.
- **`index.html` "Subjects by Grade Level" redesign:** the three plain white/bordered accordion rows were rebuilt as full-width **photo-banner triggers** — each grade band's `[data-grade-trigger]` button now has its own background photo (Primary: `photo-1714646793075-6dbfc604e664`, an adult reading with a young child; Middle: `photo-1758685733664-4cde7bbe4713`, a boy at a laptop in front of a math-covered chalkboard; High: `photo-1758525861622-f4e7ac86a2d7`, a teen with a notebook) + a directional gradient wash + a glassmorphism grade-badge and chevron-toggle circle, white text throughout. The underlying accordion mechanism (`initGradeAccordion()` in `main.js`, scoped via `row.querySelector("[data-grade-trigger]")`/`[data-grade-panel]`) needed zero JS changes since it only depends on the `data-grade-row`/`data-grade-trigger`/`data-grade-panel`/`aria-expanded`/`aria-controls` attribute contract, not on any specific visual classes — confirmed by reading the function before touching the markup. The expanded panel content (the horizontal folder-card subject strip) was left unchanged, just given a touch more top padding and a soft tinted panel background to connect visually to the banner above. A `.blob-secondary` shape was added to the section.
- **`index.html` hero redesign:** background changed from a single `texture-notebook` class to a layered `section-gradient-soft` wash + a separate `texture-notebook` overlay div (see bug note below) + a third `.blob-white` accent shape. The photo composition gained a soft blurred radial glow behind it (`z-index:0`), a second smaller accent photo tucked behind the main card's top-left corner (`photo-1488722796624-0aa6f1bb6399`, a close-up of a hand writing in a spiral notebook — deliberately chosen to reinforce the site's notebook/pencil signature motif, hidden below `sm` to avoid crowding, matching the established "hide overlapping decorative element on mobile" convention from this project's own history), and an explicit z-index stack (`glow:0 → accent photo:1 → main photo:10 → mini-cards:20`) so layering is unambiguous rather than relying on DOM-order stacking alone. The headline's highlighted word "Clicks" gained a decorative hand-drawn SVG underline (a wavy `<path>` in marigold, absolutely positioned inside a `relative inline-block` wrapper around just that word — sized in a `viewBox` so it scales with the span regardless of breakpoint or `dir`). The trust-signal line changed from plain inline text with "·" separators to three individual pill badges. The primary CTA switched from solid `.btn-write` to `.btn-write-gradient` (now that the button bug above is fixed).
- **Bug caught and fixed while building the hero:** the first draft combined `section-gradient-soft` and `texture-notebook` as two classes on the *same* element. `.texture-notebook` sets `background-image` only; `.section-gradient-soft` sets the `background` *shorthand*, which resets `background-image` — and since `.section-gradient-soft`'s rule appears later in `style.css` with equal (single-class) specificity, it would have silently deleted the ruled-line texture entirely rather than layering with it. Caught by reading the actual CSS rule order before shipping, not after. Fixed by splitting them into two separate stacked `absolute inset-0` divs, matching the layering technique `about.html`'s inner-page hero already established (`.hero-photo-layer` / `.hero-gradient-overlay` / a separate `.texture-notebook` div — three elements, never combined classes). **Lesson: two classes that both set `background`/`background-image` on the same element should never be assumed to "layer" — check whether either uses the `background` shorthand (which resets the other longhand properties) and whichever rule is later in source order silently wins outright; when in doubt, stack them as separate absolutely-positioned sibling elements instead, per the established pattern.**
- **Image sourcing:** all 8 new photos were sourced via Unsplash search → `WebFetch`-then-`Read` visual verification (one candidate rejected after visual review — a "two women high-fiving in a co-working space" photo that read as generic corporate stock rather than a K-12 tutoring scene) → cross-checked against every photo ID already present in the project via `grep -oh "photo-[a-zA-Z0-9_-]*" *.html | sort -u` before use. Post-edit verification (`grep -c` per new ID across all HTML files) confirmed every new ID appears exactly once site-wide — no duplicates introduced.
- **Verification:** confirmed balanced `<section>`/`</section>` and `<div>`/`</div>` counts in both edited files after all changes (`grep -o` counts matched exactly); confirmed the `data-grade-row`/`data-grade-trigger`/`data-grade-panel` accordion contract was preserved so `initGradeAccordion()` needed no changes.

### Post-launch fix: Home 1 and Home 2 heroes read as the same composition
Feedback after the pass above: despite different colors/content, Home 1's hero (from the previous revision) and Home 2's hero were both still a "badge → headline → paragraph → dual CTA" text block on the left paired with a single boxed visual on the right (`grid lg:grid-cols-2` vs `grid lg:grid-cols-[1.1fr_1fr]` — nearly the same shape), so they read as the same template reskinned rather than two different hero mechanics. Colors were never really the problem; the shared two-column split was.

**Fix — restructured Home 1's hero to break the two-column split entirely:** it's now a single-column, fully centered layout (badge/H1/paragraph/buttons/chips/trust-pills all centered, `max-w-3xl mx-auto text-center`) with a **horizontal fanned photo collage** placed *below* the text rather than beside it — three photos in a row (`flex items-end justify-center`), the center one upright and largest (the existing tutor+student photo), flanking it two smaller photos independently rotated ∓8° with a `translate-y-4` offset so they sit slightly lower and read as "scattered flashcards" (one is the pencil/notebook photo reused from the prior revision; the other is a newly-sourced small-group-study photo, `photo-1776178320085-bbb635750933`, verified unique and visually confirmed via the WebFetch-then-Read technique). The two signature mini-cards (Next Class / rating quote) now float over the collage row itself (positioned relative to the collage container, not the single photo), hidden below `md` since the side photos are also hidden below `sm` and there's nothing left to anchor them to at narrow widths. Home 2's hero (full-bleed dark photo + glass Quick Match form, side-by-side) was left untouched — it didn't need to change, since centered-stacked vs. side-by-side-split is now an unambiguous structural difference between the two homepages, not just a palette difference.
- **Lesson:** when two hero sections use the same brand palette and the same underlying grid shape (2-column, text-left/visual-right), differentiating them only through color, imagery, or decorative elements (blobs, gradients) is not enough to read as genuinely different — the *macro composition* (column count, alignment, stacking order) has to differ, not just the content inside it. Check this first before reaching for more decoration.

### Post-launch fix: hero requested back to a right-side image, in a modern layout
Immediate follow-up request: move the hero photo back to the right side, "modern layout." Rebuilt Home 1's hero a third time, this time as a **text-left / angled full-bleed photo panel right** split — the photo is no longer a boxed, rounded, bordered card (every prior version of this hero) but a true edge-to-edge background panel occupying the right ~54% of the section, cut with a `clip-path: polygon(14% 0, 100% 0, 100% 100%, 0% 100%)` diagonal edge instead of a straight column boundary, carrying its own dark gradient wash (for the floating info-card text) and a faint `texture-notebook` layer for brand continuity. This is deliberately still not the same shape as Home 2: Home 2's photo is full-bleed *behind the entire section* (both columns) with a *glass form* floating on top; here the photo occupies only the right panel behind an angled cut, the left column sits on the plain light `section-gradient-soft` background (not photo), and there's no form — just the two existing signature mini-cards, now floating on the photo panel itself. Desktop-only (`hidden lg:block`) — a diagonal full-bleed split doesn't degrade gracefully below `lg`, so tablet/mobile fall back to the original boxed-photo-card treatment (reusing the same corner-bracket styling from every earlier version) in the second grid column instead, with only the "Next Class" mini-card (not both) to avoid crowding a narrower viewport.
- The two photos sourced for the previous (now-superseded) fanned-collage version (`photo-1488722796624-0aa6f1bb6399`, `photo-1776178320085-bbb635750933`) are no longer referenced anywhere on the site — confirmed via grep, no dangling `<img>` tags left behind. The original hero photo (`photo-1522881193457-37ae97c905bf`) is now used twice on this single page (desktop bg panel + mobile fallback card) — treated as an exception to the no-duplicate-photos policy, same reasoning as the tutor-headshot exception: it's one responsive presentation of the same hero identity for two breakpoints, not the same photo reused across two unrelated sections.
- **Pattern for future templates:** a full-bleed diagonal-clip photo panel confined to one side of a section (as opposed to a boxed/bordered photo card, or a photo spanning the entire section behind all content) is a fourth distinct hero-image treatment for this template's history alongside the boxed flashcard-stack, the below-text fanned collage, and Home 2's full-bleed-behind-everything-plus-glass-form — reserve this specific "angled bg panel confined to one column" mechanic if any future template revision is tempted to reuse it elsewhere on this site.

### Post-launch fix: hero panel photo too washed out, wanted a richer image treatment
Follow-up request: the angled photo panel's directional gradient (`rgba(20,38,74,.6) → .08`) plus a 20%-opacity `texture-notebook` layer on top was making the photo itself read as too transparent/washed out. Lightened both — the directional wash dropped to `.32 → .02` and the texture layer to `opacity-10` — so the photo now reads clearly while the mini-cards (which have their own opaque white/ink card backgrounds, not text sitting directly on the photo) needed no legibility compensation either way. Added a new `.hero-panel-img` treatment for "some effects on the image": a `saturate(1.18) contrast(1.06)` color-grade filter for richer, punchier color, a slow one-time `hero-panel-zoom` settle-in animation (`scale(1.1) → scale(1)` over 14s ease-out, `prefers-reduced-motion` disables it), a soft `radial-gradient` vignette (darkened corners, brightest around the tutor/student subject) for photographic depth, and a 1px inset white-hairline `box-shadow` for a subtle "framed glass" edge. All layered as separate absolutely-positioned siblings inside the existing `overflow-hidden` clipped panel (not combined `background` shorthand classes on one element) — consistent with the layering lesson recorded earlier in this file.

### Confirmed site-wide bug: accordion "close" never visually collapsed the panel
User report: "Subjects by Grade Level" toggle not working. Investigated with a real headless-browser test (Playwright, not static reasoning) rather than guessing — clicking a trigger to open worked, but clicking again to close, or opening a sibling row (which auto-closes the others), left the panel's `boundingBox().height` unchanged even though `aria-expanded` correctly flipped to `false`. Root cause, found by reading every accordion-related function in `main.js`: the "close" branch of three separate accordion implementations set `panel.style.maxHeight = null` instead of `"0px"`. Setting an inline style property to `null` via JS **removes that declaration entirely** rather than collapsing it to zero — since nothing else constrains these panels' height, removing the declaration reverts `max-height` to its initial value (`none`), so the panel renders at full natural content height regardless of `aria-expanded` state. This is why the *first* open of any row looked fine (opening always takes the `scrollHeight + "px"` branch) but closing — or clicking a second row, which runs the same "close others" branch on every sibling — silently failed to collapse anything, eventually leaving multiple panels stuck visually open at once.
- **Affected every accordion on the site**, not just this section: `initAccordions()` (generic `data-accordion="single|multi"` system used on `faq.html`, `pricing.html`, `subjects.html`, `contact.html`, and this page's own FAQ preview), `initGradeAccordion()` (this section), and `initFaqSearch()`'s auto-expand-on-match feature. Fixed all three by changing every `... : null` / `= null` maxHeight assignment to `"0px"`.
- **Verified the fix with the same Playwright test** (not just re-reading the code): open → close → open-a-different-row now correctly show `maxHeight`/bounding-box height dropping back to the collapsed state each time, with zero JS console errors.
- **Lesson: `el.style.someProperty = null` is not equivalent to setting it to `"0"` or any other collapsed value — it deletes the inline declaration outright.** Any toggle that opens by setting an explicit pixel value must close by setting an explicit collapsed value (e.g. `"0px"`), not by nulling the property, unless a lower-specificity fallback rule is deliberately relied upon (it wasn't here — no other rule constrained these elements' height). **Also: when a report describes broken interactive behavior, reproduce it with a real headless-browser click-and-measure test before touching code** — static reasoning about the JS had (incorrectly) cleared this exact function as fine in an earlier pass of this file; only an actual `.click()` + `boundingBox()` check surfaced the real defect.

### Post-launch redesign: "Subjects by Grade Level" moved off the accordion pattern entirely
Rebuilt the section as a **static 3-card grid** (no expand/collapse, nothing hidden behind a click): each grade band is now an always-visible `<a>` card — a `h-48` photo header (reusing the three grade-band photos already sourced for the previous accordion version, still each used once) with a bottom gradient wash, a glass grade-badge, and a title/subtitle overlay, followed by a plain white/ink-800 body listing that band's top 4 subjects (icon + name, no description text this time — kept intentionally terse since the destination page has the full detail) and a "View All Subjects" link with an arrow that nudges on hover. Each whole card links to `subjects.html#primary|middle|high`. This removes this specific section's dependency on the (now-fixed, but historically fragile) accordion JS entirely — `initGradeAccordion()` is left defined in `main.js` (harmless, early-returns via its own `if (!rows.length) return` guard) in case a future page wants the pattern, but no page currently uses it. Verified via a second Playwright pass: all three cards present with correct `href`s and text content, zero leftover `[data-grade-trigger]`/`[data-grade-panel]` elements, zero console errors, and `<section>`/`<div>`/`<span>`/`<a>` tag counts balanced across the whole file after the edit (a sequential-edit slip briefly left orphaned markup fragments from the old accordion mid-file — caught by reading the result back before considering the task done, not assumed correct from the diff alone).

### Post-launch redesign: `subjects.html` moved to a sticky sidebar + unified grid layout
Full-page redesign per explicit request — "have card only change modern layout": every subject card's content (title, description, "what you'll learn" bullets, format tags, per-subject trial CTA) was kept verbatim; only the page's structural layout changed. Rebuilt the whole file directly with the `Write` tool in one pass (not sequential `Edit` calls) given the scale of the restructure, to avoid the kind of orphaned-fragment risk noted above.
- **Old structure:** hero (with 3 grade-jump chip links) → a separate full-width "How to Read This Page" intro band (paragraph + 3 icon-stat blocks) → a horizontally-wrapped pill-button filter bar (10 buttons, one flat row) → three separate full-width `<section>`s, one per grade band, each with its own heading row and its own `sm:grid-cols-2 lg:grid-cols-3` card grid.
- **New structure:** hero (simplified — breadcrumb/title/subtitle plus a compact inline 3-stat line, no more chip links since the sidebar supersedes them) → one `lg:grid-cols-[280px_1fr]` two-column layout: a **sticky filter sidebar** (`lg:sticky lg:top-28`) on the left with an "All Subjects" button, a labeled "Grade Band" button group, a labeled "Subject Area" button group, and a small folder-card trial-class prompt — and a **single unified card grid** on the right containing all 19 cards together (no more three separate full-width grade sections). Each card gained a small grade-band badge pill (top corner, `absolute`, bidi-aware `rtl:right-auto rtl:left-4`) since grade context is no longer implied by which section a card sits under.
- **Filter mechanics preserved, "no results" logic fixed to match:** the sidebar's buttons still use the exact same `data-filter` values and single-select `aria-selected` click-wiring as before (`initSubjectFilter()` in `main.js` needed no changes to its click-handling), so the sidebar is a pure visual/structural reorganization of the same 10 buttons, not a new filter engine. However, the function's "no results" empty-state detection previously worked by checking whether any `[data-grade-row]` *section wrapper* still had visible cards inside it — since those section wrappers no longer exist on this page, that logic would have permanently shown "no results" regardless of actual matches. Fixed by computing visibility directly from the card list itself (a `visibleCount` counter incremented inside the existing `cards.forEach` loop, mirroring `initTutorFilter()`'s simpler pattern) instead of proxying through the now-removed rows. Verified via Playwright: `filter=primary` → 6 visible, `filter=coding` → 3 visible (primary+middle+high each have one coding card), `filter=all` → all 19 visible, exactly one button ever carries `aria-selected="true"`, zero console errors.
- **Data-accuracy fix caught in passing:** the old intro band claimed "20 Subject Tracks" — the actual card count is 19 (6 Primary + 6 Middle + 7 High). Corrected the number in the new hero stat line while rebuilding the page, rather than carrying the pre-existing inaccuracy forward.
- **Verified:** `<section>`/`<div>`/`<span>` tag counts balanced across the whole file; zero leftover `data-grade-row` references; dark mode and RTL both toggle correctly; no horizontal overflow at 375/768/1440px (Playwright `scrollWidth - clientWidth` check, all zero).

### Post-launch: pagination added to `subjects.html`, catalog grown to 21 cards for clean 3/6/9 paging
Request: show the grid as 3×3, paginate the remainder, and every page must total 3, 6, or 9 cards (a full-row multiple, never a stray 1-2-card tail). With the catalog at 19 cards, 19 is not evenly divisible into any page size that's itself a multiple of 3 (19 mod 3 = 1 regardless of whether the page size is 3, 6, or 9) — no pagination scheme could satisfy the constraint without changing the dataset. Rather than accept a stray last card, grew the catalog to a symmetric **21 cards (7 Primary + 7 Middle + 7 High)**, which pages perfectly into 9 / 9 / 3.
- **Two new subject cards added**, both fully fleshed out to match the existing card format (icon, description, 3 "what you'll learn" bullets, format tags, per-subject trial CTA) — not placeholders:
  - **Primary → "World Languages Introduction"** (`data-filter-subject="languages"`). Primary was previously the only grade band with no Languages offering at all, so this also closes a genuine content gap rather than just padding the count.
  - **Middle → "Public Speaking & Debate"** (`data-filter-subject="english"`, since it's a language-arts-adjacent skill area). This also happened to bring the site-wide "english" filter count to a clean 6.
  - Total is now 21; hero's "Subject Tracks" stat corrected from 19 → 21.
- **Pagination** (`initSubjectFilter()` in `main.js`, extended rather than replaced): page size is read from `data-page-size="9"` on the grid container (`#subject-grid`) so it's configurable without touching JS. On each render, the currently-matching card set (same filter logic as before) is sliced into the active page's window; cards outside that window get `.hidden` alongside cards that don't match the filter at all — one unified visibility pass, not two competing ones. Changing the active filter always resets to page 1. Prev/Next buttons (`data-page-prev`/`data-page-next`) disable at the boundaries; a `data-page-indicator` shows "Page X of Y"; the whole pagination `<nav>` auto-hides via `totalPages <= 1` whenever a filtered result fits on a single page (every filtered subject-area/grade-band view except "All Subjects" is ≤9 cards, so in practice pagination only ever engages on the default "All Subjects" view — confirmed this is expected, not a bug, since a single page never needs page controls).
- **Verified via Playwright:** "All Subjects" → 9 / 9 / 3 across 3 pages with correct prev/next disabled states at both ends; switching to a small filter (e.g. Primary, 7 cards) hides the pagination nav entirely; switching back to "All Subjects" resets to page 1; dark mode, RTL, and zero horizontal overflow at 375/768/1440px all still hold; zero console errors.

### Post-launch: catalog trimmed back to 18 cards (6/6/6), even cleaner pagination
Follow-up request: remove one card each from Primary, Middle and High School, plus remove a math-tagged card. Since this was genuinely ambiguous (4 stated removals could mean 4 separate cards, landing at 17 — which would *break* the 3/6/9 pagination just built, since 17 isn't divisible by 3 either) a clarifying question was asked before touching anything; the user confirmed **3 total removals**, with the High School removal specifically being a math-tagged card.
- Removed **"World Languages Introduction"** (Primary) and **"Public Speaking & Debate"** (Middle) — these were exactly the two cards added in the prior pass purely to pad Primary/Middle from 6→7 for the (now superseded) 21-card pagination scheme, so removing them cleanly reverts each band to its original 6 core subjects rather than cutting into content that predates this whole pagination effort.
- Removed **"SAT / ACT Test Prep"** (High School) — this card was double-tagged `data-filter-subject="math english"`, making it the one card responsible for the "math" filter view showing a non-clean count of 4 instead of 3 (every other subject-area filter was already exactly 3 or a multiple of 3). Removing it fixes that inconsistency as a side effect of satisfying the High-School removal.
- **Net result: 18 cards (6 Primary + 6 Middle + 6 High), fully symmetric.** This pages *more* cleanly than the previous 21-card version — 18 ÷ 9 = exactly 2 pages of 9, with no partial third page at all (versus 21's 9/9/3). Hero's "Subject Tracks" stat corrected 21 → 18.
- **Verified via Playwright:** total card count is 18; "All Subjects" pages as exactly 9 then 9 with `next` correctly disabled on page 2; `math` filter now shows exactly 3 (was 4); primary/middle/high grade filters each show exactly 6; switching back to "All Subjects" resets to page 1; body text contains no leftover references to any of the three removed cards' titles; no horizontal overflow at 375/768/1440px; zero console errors.
- **Lesson reinforced:** when a user's removal/addition request is phrased ambiguously and the ambiguity changes a concrete downstream number (here: 17 vs 18 total cards, and whether the just-built pagination constraint would hold or break), asking a short clarifying question before editing is worth it — guessing wrong on a destructive content change costs more than one extra round-trip.

### Post-launch: `subjects.html` visual/interaction modernization pass
Request: "redesign use modern design and layout." The sidebar+grid+pagination *structure* from the earlier pass was already modern; what was still plain was the visual treatment (flat white sidebar buttons, no live feedback, no card interactivity) — so this pass focused on richer visuals and interaction rather than another structural rebuild.
- **Sidebar rebuilt as an elevated glass-style panel**: wrapped in a `rounded-2xl` white/ink-800 card with shadow + border; every filter button gained a colored icon circle (`filter-icon-circle` — primary-tinted for grade-band buttons, secondary-tinted for subject-area buttons) and a live result-count badge (`filter-count`, hardcoded per button since the underlying data is static — All 18, Primary/Middle/High 6 each, Math/Science/Social Studies/Coding 3 each, English 4, Languages 2). New `.subject-filter-btn` CSS class replaces the old inline Tailwind `aria-selected:` arbitrary-variant styling with a **left accent bar** (`::before`, bidi-aware via `[dir="rtl"]` override) plus an inverted (solid-fill) icon circle and amber count badge on the active button — a proper "app sidebar" active-state pattern.
- **Mobile-responsive filter panel**: below `lg`, the sidebar is `hidden` by default behind a new "Filter Subjects" toggle button (`[data-filter-toggle]`, `aria-expanded`-driven, chevron rotates via the existing `.accordion-chevron` mechanism) instead of always stacking above the grid and pushing 21 buttons' worth of content down the page before any subject card is visible — a real mobile UX fix, not just decoration.
- **Active-filter summary bar**: new "Showing N results for [Label] · Clear filter" line above the grid, only visible when a non-"all" filter is active. Reads the active button's own `[data-filter-label]` span (added inside every button specifically so the summary can grab a clean label without also picking up the icon or count-badge text) rather than hardcoding a label map in JS. "Clear filter" re-triggers the existing "All Subjects" button's own click handler rather than duplicating its logic.
- **Cards gained `data-tilt`** (the site's existing pointer-driven 3D tilt utility, already used elsewhere — applied here via one `replace_all` since all 18 cards shared an identical class-string insertion point) for a subtle modern hover interaction; gated by the shared `initTilt()` guard (`pointer:fine` + no-reduced-motion), so it's inert on touch devices.
- **Two decorative blobs** added to the catalog section for atmosphere, consistent with the rest of the site's richer-visual-system vocabulary.
- **Bug avoided before shipping:** the summary bar's show/hide was first written as `classList.remove('hidden'); classList.add('flex')` on show but only `classList.add('hidden')` (no `flex` removal) on hide — meaning after the *second* show/hide cycle both `.hidden` and `.flex` utility classes would be present simultaneously, and which one wins is a Tailwind-internal cascade-order question, not something to rely on. Caught this while writing the JS (not after testing found it broken) and fixed by never toggling a second display-utility class at all: added `[data-subject-summary]:not(.hidden){display:flex}` to `style.css` so the *only* class JS ever needs to toggle is `hidden`, matching the `.hidden`-only toggle pattern used everywhere else in this codebase. **Lesson: toggling two independent display-related classes (`hidden` + `flex`) on the same element across repeated show/hide cycles is fragile — express "flex when shown" as a single CSS rule keyed off the absence of `.hidden`, so JS only ever has one class to manage.**
- **Verified via Playwright** (separate desktop 1400px and mobile 390px browser contexts): desktop sidebar always visible / mobile toggle hidden; filtering (Science → 3 results) correctly shows the summary bar with the right count and label; Clear correctly resets to "All Subjects" with the All button's `aria-selected` restored and pagination back to page 1 of 2; pagination Next still works after the sidebar rebuild; dark mode and RTL both still correct; zero horizontal overflow at 1440/1024px (desktop) and 390px (mobile); mobile sidebar toggle correctly hidden→shown with `aria-expanded` flipping; zero console errors in both browser contexts. Also visually confirmed via full-page screenshots in both light and dark mode.

### Post-launch: `tutors.html` card redesign + "balanced count" grid fix
Request: redesign the tutor cards, "modern and balanced count layout." Investigated first rather than guessing: with 9 tutors in a fixed `grid sm:grid-cols-2 lg:grid-cols-3` grid, "All Tutors" (9) is already balanced, but filtering by a single subject/grade could leave an incomplete, left-stuck last row — the same underlying class of problem as `subjects.html`'s earlier pagination issue, just manifesting through filtering instead of pagination.
- **Rejected inventing extra fictional tutors just to force every filter count to a multiple of 3** (the fix applied on `subjects.html`) — for single-specialist filters like Coding (1 tutor) or Languages (1 tutor), padding the roster with 2 more invented Coding/Language specialists each would dilute the "every tutor is a real specialist, not padding" narrative the team-stat band and hero copy already establish. Used a **CSS layout fix instead**: the card container changed from `grid` to `flex flex-wrap justify-center`, with each card given an explicit responsive width (`w-full sm:w-[calc((100%-1.75rem)/2)] lg:w-[calc((100%-3.5rem)/3)]`, matching the original 1/2/3-column breakpoints exactly). Unlike CSS Grid (which always left-aligns/stretches a short last row), flexbox with `justify-content:center` automatically centers any incomplete row of remaining cards — so a single-card filter result (Coding, Languages, Social Studies) now renders as one centered card instead of one card stranded at the left edge with a large empty gap beside it. No JS changes needed; `initTutorFilter()` only ever toggles `.hidden`, agnostic to how the container lays out visible children.
- **Discovered while verifying:** Meera Joshi's card is triple-tagged `data-filter-subject="math english science"` (she's the Primary Grades generalist), which means the Math, Science, and English filters all land on exactly 3 results each already — better balance than initially assumed before checking. Only Social Studies, Coding, and Languages (each a genuine single-specialist filter) ever produce a 1-card result; verified via Playwright that **every** filter combination (1, 3, 4, 6, 7, and 9-card results were all exercised) renders as a horizontally centered group.
- **Card redesign:** moved from a padded card with an inset `rounded-lg` thumbnail + plain-text rating/availability lines, to an edge-to-edge photo card — the photo now fills the full card width with a bottom gradient wash, a glass rating badge (`★ 4.9`, single icon + number, not five repeated star glyphs) floating top-corner on the photo, and the weekly availability line overlaid directly on the photo in white text. Text content (name, qualification, subjects, teaching-method chips) moved into a separate padded `.p-6` block below. The signature `folder-tab-icon` notch stays on the outer (unclipped) card as before; only a new inner `overflow-hidden rounded-tr-2xl` wrapper around the photo clips the image to match the card's rounded corner — deliberately *not* adding `overflow-hidden` to the outer card itself, which would have clipped the tab-notch that renders slightly outside the card's own box. **Lesson carried forward from the `subjects.html` grade-banner redesign: when a folder-card-family component needs an edge-to-edge clipped photo, clip an inner wrapper, never the outer `.folder-card` itself, or the signature tab-notch silently disappears.**
- **Verified via Playwright:** 9 total cards; every one of the 9 filter buttons (all/math/science/english/social-studies/coding/languages/primary/middle/high) produces a horizontally-centered result group regardless of count; the shared tutor bio modal still opens correctly from the redesigned cards (unaffected, since it reads the same untouched `data-*` attributes); dark mode, RTL, and zero horizontal overflow at 375/768/1024/1440px all hold; zero console errors. Visually confirmed via screenshots — both the full "All Tutors" 3×3 grid and a single centered card under the Coding filter.

### Post-launch: `tutors.html` full-page redesign — hero + stat band
Follow-up request: "redesign tutors.html page" (broad, whole-page). Since the card grid and filter section had just been redesigned the prior pass, this round targeted the two sections that hadn't been touched yet: the hero and the "Meet the Team" stat band — both still the generic flat photo-hero + solid-dark-band formula shared with every other inner page.
- **Hero rebuilt as a split layout**: text (breadcrumb/H1/subtitle/trust-badge row) on the left, an **avatar-stack photo collage** on the right — four of the team's own tutor headshots (Kavya, Priya, Aditya, Sofia — reusing their existing card photos, the established allowed exception to the no-duplicate-photos policy since it's the same people recurring in a "meet the team" teaser) overlapping in a staggered circular row, closed with a `+38 More` badge circle (42 total tutors minus the 4 shown). This ties the hero directly to the page's actual purpose (browse tutors) rather than a generic stock photo background, and is a structurally different hero mechanic from every other inner page on the site.
- **Stat band converted from a flat solid-color band into a floating glass card row**: a white/ink-800 `rounded-2xl` card with an internal `divide-x`/`divide-y` grid (4 stat cells, each with an icon-circle + count-up number + label) positioned with `-mt-14 sm:-mt-16` to overlap the hero's bottom edge — the hero's own bottom padding was increased (`pb-14` → `pb-24 lg:pb-28`) to make room for the overlap. This is a common modern "elevated stat card straddling a hero/content seam" pattern, distinct from the flat `section-gradient-deep` band used elsewhere.
- **Light enrichment**: added a `.blob-primary` decorative shape to the "Our Teaching Methods" section (previously a plain flat-tint band with no decoration) for consistency with the richer-visual-system vocabulary used elsewhere on the site.
- **Verified via Playwright + full-page screenshots** in light mode, dark mode, and RTL: the hero/stat-card overlap renders cleanly in all three (confirmed visually, not just via DOM assertions, since a manual negative-margin overlap technique is exactly the kind of thing that can look right in the DOM inspector but wrong on screen); avatar-stack margins and stat-card grid mirror correctly under `dir="rtl"`; zero horizontal overflow at 375/768/1024/1440px; zero console errors.

### Post-launch: `tutors.html` roster expanded so every subject filter has at least 3 tutors
Request: every subject in "Filter by Subject or Grade Band" should have at least 3 cards. Before this pass, Math/Science/English were already at 3 (Meera Joshi's card is triple-tagged `math english science`, quietly covering all three), but Social Studies, Coding and Languages each had exactly one tutor (Daniel, Aditya, Sofia respectively).
- **Rejected tagging existing tutors with extra subjects to hit the number** — e.g. adding "coding" to Rohan's (Math) card would pad the count without him actually being a coding specialist, which conflicts with the hero's own "certified specialist in their subject — not a generalist" promise. Added **6 new, fully-fleshed-out tutor profiles instead** (2 per underrepresented subject), bringing the roster shown on this page from 9 to 15 (out of the "42+" total the stat band already claims, so no inconsistency there):
  - **Coding:** Wei Chen (Scratch/robotics, Primary·Middle) and Fatima Malik (web development, High School) — deliberately different grade bands and sub-specialties from the existing Aditya Rao (Python/AP CS, Middle·High), so the three coding tutors don't overlap identically.
  - **Languages:** Lucas Bernard (French, Middle·High) and Isabella Torres (Spanish, Primary·Middle) — complementing Sofia Martins (Spanish/French, Middle·High) with primary-grade coverage and a dedicated French specialist, both languages staying consistent with the two languages `subjects.html` actually offers (no new language invented just for headcount).
  - **Social Studies:** Marcus Webb (AP History/Government, High School) and Grace Kim (Geography/Civics, Middle School) — complementing Daniel Cross (History·English, Primary·Middle) with high-school AP coverage the roster didn't have before.
  - All 6 have complete profiles matching the established card template exactly (qualification, years, rating, availability, teaching-method chips, full bio) — no placeholder content.
- **Image sourcing:** 6 new photos sourced via Unsplash-search → WebFetch-then-Read visual verification, cross-checked against every photo ID already on the site. One candidate (a woman at a desk with a chalkboard full of math/chemistry equations behind her) was rejected for the Languages/Social-Studies roles specifically *because* of the equation-covered background — using a visibly math-themed photo for an unrelated subject would have been exactly the kind of content/caption mismatch this project's registry has flagged before as a real defect, not just a style nitpick.
- **Verified via Playwright:** all 6 subject-area filters (math/science/english/social-studies/coding/languages) now return exactly 3 results each; grade-band filters recompute correctly (primary 6, middle 11, high 9); every filter combination still renders as a horizontally centered group (the flex-wrap centering fix from the prior pass required zero changes to keep working at the new counts); the shared tutor modal opens correctly for a newly-added card (Wei Chen); dark mode, RTL, and zero horizontal overflow at 375/768/1024/1440px all confirmed; zero console errors; confirmed no duplicate photos were introduced anywhere on the site outside the standard per-card `data-img`+`<img src>` pairing every tutor already has.

### Post-launch: Middle School count trimmed by 2 + pagination added to `tutors.html`
Request: reduce Middle School by 2 cards, and paginate "All Tutors" at 3×3 (9) per page.
- **Reduced Middle by de-tagging, not deleting**: rather than deleting 2 whole tutor cards (which would have reduced the total roster and disturbed the just-established "every subject filter has ≥3" guarantee, since grade and subject tags are independent but a deleted card loses both), removed just the `middle` portion of two dual-tagged cards' `data-filter-grade`: **Aditya Rao** and **Sofia Martins**, both previously `"middle high"`, now `"high"` only (their `data-grades` display string updated to match: `"High School"`). Chosen specifically because it's also thematically accurate, not arbitrary — Aditya teaches AP Computer Science (an explicitly high-school program) and Isabella Torres/Lucas Bernard already cover Spanish/French for Primary and Middle, so narrowing Sofia to High School removes redundant middle-grade overlap rather than a real coverage gap. Result: Middle School count 11 → 9 (itself a clean multiple of 3), total roster unchanged at 15, and every subject-area filter still returns exactly 3 (untouched, since only the grade tag changed).
- **Pagination added** (`initTutorFilter()` extended with the same page-size/prev-next/indicator pattern already established on `subjects.html`'s `initSubjectFilter()` — `data-page-size="9"` on `#tutor-grid`, a `[data-tutor-pagination]` nav with `data-page-prev`/`data-page-next`/`data-page-indicator`): "All Tutors" (15 cards) now pages as a clean **9 + 6**, both multiples of 3, no partial row on either page. Filter changes reset to page 1. The pagination nav auto-hides whenever a filtered result already fits on one page — which, after the Middle trim, is now every single filter on this page (Middle=9, High=9, subjects=3 each, Primary=6) — so in practice pagination currently only ever activates on the default "All Tutors" view, matching `subjects.html`'s same behavior.
- **Verified via Playwright:** 15 total cards; "All" → 9 then 6 across 2 pages with correct prev/next disabled states at both ends; Middle → exactly 9, pagination hidden; all 6 subject filters still exactly 3 each with pagination hidden; switching back to "All" resets to page 1; page 2's 6-card result still renders as a horizontally centered group (the flex-wrap centering fix continues to compose correctly with pagination, no interaction bugs between the two mechanisms); the shared tutor modal still opens correctly; dark mode, RTL, and zero horizontal overflow at 375/768/1024/1440px all hold; zero console errors.

### Post-launch: `subjects.html` complete redesign #3 — top toolbar + horizontal list cards
Request: "completely redesign, use modern layout." This is the third structural pass on this page (original stacked-sections → sidebar+grid+pagination → this). Since the sidebar+grid macro-layout had already been built, tested and visually polished over two prior passes, a genuine "complete redesign" this time meant changing the layout paradigm itself, not just re-skinning the same sidebar. Rebuilt the whole file with `Write` (not sequential edits, given the scale) while preserving every subject's exact content (title, description, 3 bullets, format tags, CTA) verbatim.
- **Sidebar → full-width top toolbar**: the left `lg:sticky` sidebar is gone. In its place, a single elevated card at the top of the content area holds "All Subjects" plus two inline groups (Grade Band, Subject Area) laid out horizontally (`flex-col` stacking to `lg:flex-row`), each filter still showing its live count badge. This frees the full page width for content and reads as a more "toolbar/marketplace" pattern (Airbnb/Udemy-style category bar) rather than an app-sidebar.
- **Vertical icon-tab cards → horizontal list cards**: each subject card changed from a top-icon vertical `folder-card` to a horizontal row (large colored icon-square on the left, title/description/bullets/tags/CTA on the right) in a `lg:grid-cols-2` layout. Deliberately **not** using the `folder-card` class for these — that component's asymmetric `border-radius: 0 1rem 1rem 1rem` exists specifically to make room for the folder-tab-notch signature, which doesn't translate to a horizontal layout; forcing it in would have left a stray unstyled sharp corner with no visual explanation. Used a plain `rounded-2xl` card instead, still consistent with the site's broader shadow/hover-lift language, with `data-tilt` preserved for the pointer-tilt interaction.
- **Pagination page size changed 9 → 6** to match the new 2-column (not 3-column) grid — `data-page-size="6"` on `#subject-grid`, zero JS changes needed since `initSubjectFilter()`'s pagination logic already reads the page size from that attribute dynamically. With 18 total cards this now pages as a *maximally* clean **6 + 6 + 6** (three full pages, not even a shorter final page) — cleaner than either prior pagination scheme on this page.
- **New CSS added** (`.subject-filter-btn-pill`): the old `.subject-filter-btn` sidebar-row styling (left-accent-bar active indicator, full-width rows) doesn't suit inline pill buttons, so a new pill-shaped active/hover state was added rather than stretching the old component to fit a shape it wasn't designed for. The old `.subject-filter-btn`/`.filter-icon-circle` rules are left in place or as it was found to be genuinely unused, `.filter-count` is deliberately shared/reused by both old and new button styles.
- **Verified via Playwright:** 18 total cards; "All Subjects" pages as exactly 6/6/6 with correct next-disabled state on page 3; every grade-band filter (6 each) and subject-area filter (3/3/4/3/3/2) fits on one page with pagination correctly hidden; Clear Filter resets to "All" + page 1; toolbar's Grade Band/Subject Area group labels render; dark mode, RTL, and zero horizontal overflow at 375/768/1024/1440px all hold; zero console errors. Also visually confirmed via full-page screenshots in both light and dark mode.
